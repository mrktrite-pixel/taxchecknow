"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: can-eot-exit · Tier 1 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "eot-01",
    "name": "Your EOT Eligibility Assessment",
    "desc": "Your specific eligibility across the four core EOT conditions.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "eot-02",
    "name": "Structural Requirements Checklist",
    "desc": "Full structural checklist: vendor side + business side + EOT side.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "eot-03",
    "name": "Exemption Quantum and LCGE Interaction",
    "desc": "How the $10M exemption interacts with LCGE across single and multi-vendor scenarios.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "eot-04",
    "name": "Failure Points and Pre-Sale Fixes",
    "desc": "The six most common failure points and how to fix each before transaction.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "eot-05",
    "name": "Your Adviser Brief — EOT Transaction",
    "desc": "Questions for your Canadian tax and legal team before structuring the EOT exit.",
    "tier": 1
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessAssess() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2026-12-31T23:59:59.000-05:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const ownership_structure = sessionStorage.getItem("can-eot-exit_ownership_structure") || "individual_direct";
      const is_ccpc = sessionStorage.getItem("can-eot-exit_is_ccpc") || "yes_active";
      const holding_period = sessionStorage.getItem("can-eot-exit_holding_period") || "24_months_plus";
      const within_window = sessionStorage.getItem("can-eot-exit_within_window") || "yes_in_window";
      const eot_being_established = sessionStorage.getItem("can-eot-exit_eot_being_established") || "planning";
      const estimated_gain = sessionStorage.getItem("can-eot-exit_estimated_gain") || "3m_to_10m";
      const other_vendors = sessionStorage.getItem("can-eot-exit_other_vendors") || "sole_vendor";
      const business_type = sessionStorage.getItem("can-eot-exit_business_type") || "active_non_professional";
      const potential_exemption = sessionStorage.getItem("can-eot-exit_potential_exemption") || "6500000";
      const estimated_tax_saved = sessionStorage.getItem("can-eot-exit_estimated_tax_saved") || "1625000";
      const eligibility_outcome = sessionStorage.getItem("can-eot-exit_eligibility_outcome") || "LIKELY ELIGIBLE";
      const status = sessionStorage.getItem("can-eot-exit_status") || "LIKELY ELIGIBLE — STRUCTURE REVIEW NEEDED";
      const tier = sessionStorage.getItem("can-eot-exit_tier") || "147";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "ownership_structure": ownership_structure,
        "is_ccpc": is_ccpc,
        "holding_period": holding_period,
        "within_window": within_window,
        "eot_being_established": eot_being_established,
        "estimated_gain": estimated_gain,
        "other_vendors": other_vendors,
        "business_type": business_type,
        "potential_exemption": potential_exemption,
        "estimated_tax_saved": estimated_tax_saved,
        "eligibility_outcome": eligibility_outcome,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "individual_direct");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "can-eot-exit",
          market:     "Canada",
          authority:  "Canada Revenue Agency (CRA)",
          tier:       1,
          name,
          inputs: {
        "Share ownership structure": ownership_structure,
        "Is a CCPC": is_ccpc,
        "Shares held": holding_period,
        "Within 2024-2026 window": within_window,
        "EOT being established": eot_being_established,
        "Estimated capital gain": estimated_gain,
        "Other vendors selling": other_vendors,
        "Business type": business_type,
        "Potential exempt gain": potential_exemption,
        "Estimated tax saved": estimated_tax_saved,
        "EOT eligibility": eligibility_outcome,
        "Verdict status": status,
        "Tier purchased": tier,
          },
          fields: ["eotEligibilityAssessment","structuralRequirementsStatus","potentialExemptionAmount","estimatedTaxSaved","lcgeInteraction","identifiedFailurePoints","preRestructuringRequired","windowUrgency","immediateActions"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        eotEligibilityAssessment: "Your personalised eotEligibilityAssessment is being prepared — please refresh in a moment.",
        structuralRequirementsStatus: "Your personalised structuralRequirementsStatus is being prepared — please refresh in a moment.",
        potentialExemptionAmount: "Your personalised potentialExemptionAmount is being prepared — please refresh in a moment.",
        estimatedTaxSaved: "Your personalised estimatedTaxSaved is being prepared — please refresh in a moment.",
        lcgeInteraction: "Your personalised lcgeInteraction is being prepared — please refresh in a moment.",
        identifiedFailurePoints: "Your personalised identifiedFailurePoints is being prepared — please refresh in a moment.",
        preRestructuringRequired: "Your personalised preRestructuringRequired is being prepared — please refresh in a moment.",
        windowUrgency: "Your personalised windowUrgency is being prepared — please refresh in a moment.",
        immediateActions: "Your personalised immediateActions is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact Canada Revenue Agency (CRA) position based on my answers?",
          "What is the single most important action I should take before 31 December 2026?",
          "Are there any planning opportunities specific to my situation?",
        ],
        
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const ownership_structure = sessionStorage.getItem("can-eot-exit_ownership_structure") || "individual_direct";
    const is_ccpc = sessionStorage.getItem("can-eot-exit_is_ccpc") || "yes_active";
    const holding_period = sessionStorage.getItem("can-eot-exit_holding_period") || "24_months_plus";
    const within_window = sessionStorage.getItem("can-eot-exit_within_window") || "yes_in_window";
    const eot_being_established = sessionStorage.getItem("can-eot-exit_eot_being_established") || "planning";
    const estimated_gain = sessionStorage.getItem("can-eot-exit_estimated_gain") || "3m_to_10m";
    const other_vendors = sessionStorage.getItem("can-eot-exit_other_vendors") || "sole_vendor";
    const business_type = sessionStorage.getItem("can-eot-exit_business_type") || "active_non_professional";
    const potential_exemption = sessionStorage.getItem("can-eot-exit_potential_exemption") || "6500000";
    const estimated_tax_saved = sessionStorage.getItem("can-eot-exit_estimated_tax_saved") || "1625000";
    const eligibility_outcome = sessionStorage.getItem("can-eot-exit_eligibility_outcome") || "LIKELY ELIGIBLE";
    const status = sessionStorage.getItem("can-eot-exit_status") || "LIKELY ELIGIBLE — STRUCTURE REVIEW NEEDED";
    const tier = sessionStorage.getItem("can-eot-exit_tier") || "147";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:Canada EOT Exit Optimizer — Deadlines`,
      "BEGIN:VEVENT",
      `UID:eot-window-close-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20261231"}`,
      `DTEND;VALUE=DATE:${"20261231"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:EOT qualifying window closes — 31 December 2026",
      "DESCRIPTION:Last day for qualifying dispositions under s56.3 (as currently legislated).",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:eot-t1-apr30-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270430"}`,
      `DTEND;VALUE=DATE:${"20270430"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Canadian T1 return — 30 April 2027 (2026 tax year)",
      "DESCRIPTION:Claim EOT exemption on Schedule 3 if sale in 2026.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:eot-planning-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(90)}`,
      `DTEND;VALUE=DATE:${relativeDate(90)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:EOT planning review — 90 days",
      "DESCRIPTION:Review eligibility + restructuring + EOT establishment progress.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "can-eot-exit.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your EOT Eligibility Report — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your EOT Eligibility Report · $67
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your EOT Eligibility Report
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your personalised assessment — built around your exact answers, not a generic guide.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 31 December 2026</span>
            <span className="font-mono text-sm font-bold text-white">31 Dec 2026</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against Canada Revenue Agency (CRA) rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your Canada Canada Revenue Agency (CRA) position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["eotEligibilityAssessment","structuralRequirementsStatus","potentialExemptionAmount","estimatedTaxSaved","lcgeInteraction","identifiedFailurePoints"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* FIRST ACTION — tier 1 */}
            {assessment.firstAction && (
              <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your first action
                </p>
                <p className="text-lg font-bold leading-relaxed text-white">
                  {String(assessment.firstAction)}
                </p>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">EOT qualifying window closes — 31 December 2026</p>
                    <p className="text-xs text-neutral-500">Last day for qualifying dispositions under s56.3 (as currently legislated).</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    31 Dec 2026
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Canadian T1 return — 30 April 2027 (2026 tax year)</p>
                    <p className="text-xs text-neutral-500">Claim EOT exemption on Schedule 3 if sale in 2026.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    30 Apr 2027
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">EOT planning review — 90 days</p>
                    <p className="text-xs text-neutral-500">Review eligibility + restructuring + EOT establishment progress.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your five personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific Canada Revenue Agency (CRA) position.
                Start with File 02 — it has your exact numbers.
                
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/can/can-eot-exit/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                
                {daysToDeadline} days to 31 December 2026.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>


            {/* UPGRADE */}
            <div className="no-print rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Want the full implementation plan?</p>
              <p className="mb-1 font-serif text-lg font-bold text-neutral-950">Your EOT Exit Strategy System</p>
              <p className="mb-3 text-sm text-neutral-600">Complete EOT transaction system: pre-sale restructuring plan, EOT establishment roadmap with legal counsel coordination, vendor financing + governance framework, multi-vendor allocation strategy, and transaction timing optimisation within the 2024-2026 window.</p>
              <Link href="/can/check/eot-exit-optimizer"
                className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950 transition">
                Upgrade — $147 →
              </Link>
            </div>

            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Leaving Canada? Cross-check departure tax</p>
              <p className="mb-2 text-xs text-neutral-600">If you are also considering leaving Canada after the EOT sale, departure tax under s128.1 may apply to your remaining portfolio. Large liquidity events and emigration often coincide. Use the Departure Tax Trap auditor alongside this EOT analysis.</p>
              <Link href="/can/check/departure-tax-trap" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                Departure Tax Trap Auditor →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified Canada tax adviser before making financial decisions.
            Based on Canada Revenue Agency (CRA) guidance April 2026.{" "}
            <a href="https://www.canada.ca/en/department-finance/news/2023/03/employee-ownership-trusts.html" target="_blank" rel="noopener noreferrer" className="underline">Department of Finance — Employee Ownership Trusts</a> · <a href="https://laws-lois.justice.gc.ca/eng/acts/i-3.3/section-56.3.html" target="_blank" rel="noopener noreferrer" className="underline">Income Tax Act (Canada) s56.3</a>
          </p>
        </div>

      </main>
    </div>
  );
}
