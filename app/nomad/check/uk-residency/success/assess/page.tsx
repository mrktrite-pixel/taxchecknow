"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: uk-residency · Tier 1 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "ukr-01",
    "name": "Your UK Residency Decision — SRT Analysis",
    "desc": "Specific SRT outcome, which test resolved it, and why.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "ukr-02",
    "name": "Tie-by-Tie Assessment",
    "desc": "Which UK ties apply to you and why — with HMRC's specific definitions.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "ukr-03",
    "name": "Filing Obligations if UK Resident",
    "desc": "What to file with HMRC if the SRT makes you UK resident.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "ukr-04",
    "name": "Split Year Treatment Guide",
    "desc": "Cases 1-8 of split year treatment — when the tax year can be split into resident and non-resident portions.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "ukr-05",
    "name": "Your Accountant Brief — UK Residency",
    "desc": "Questions for your UK tax advisor before filing or taking a residency position.",
    "tier": 1
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessAssess() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2027-01-31T23:59:59.000+00:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const uk_days = sessionStorage.getItem("uk-residency_uk_days") || "46_to_90";
      const previously_uk_resident = sessionStorage.getItem("uk-residency_previously_uk_resident") || "yes";
      const ties_count = sessionStorage.getItem("uk-residency_ties_count") || "3";
      const ties_detail = sessionStorage.getItem("uk-residency_ties_detail") || "family_accommodation_work";
      const aot_applies = sessionStorage.getItem("uk-residency_aot_applies") || "no";
      const art_applies = sessionStorage.getItem("uk-residency_art_applies") || "no";
      const residency_outcome = sessionStorage.getItem("uk-residency_residency_outcome") || "UK RESIDENT";
      const status = sessionStorage.getItem("uk-residency_status") || "UK RESIDENT — SUFFICIENT TIES";
      const tier = sessionStorage.getItem("uk-residency_tier") || "147";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "uk_days": uk_days,
        "previously_uk_resident": previously_uk_resident,
        "ties_count": ties_count,
        "ties_detail": ties_detail,
        "aot_applies": aot_applies,
        "art_applies": art_applies,
        "residency_outcome": residency_outcome,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "46_to_90");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "uk-residency",
          market:     "United Kingdom",
          authority:  "HM Revenue & Customs (HMRC)",
          tier:       1,
          name,
          inputs: {
        "UK days this year": uk_days,
        "Previously UK resident": previously_uk_resident,
        "Number of UK ties": ties_count,
        "Which ties": ties_detail,
        "AOT applies": aot_applies,
        "ART applies": art_applies,
        "SRT outcome": residency_outcome,
        "Verdict status": status,
        "Tier purchased": tier,
          },
          fields: ["srtOutcome","automaticTestApplied","tiesAnalysis","ukDayCount","worldwideIncomeObligation","filingRequirements","splitYearEligibility","immediateActions"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        srtOutcome: "Your personalised srtOutcome is being prepared — please refresh in a moment.",
        automaticTestApplied: "Your personalised automaticTestApplied is being prepared — please refresh in a moment.",
        tiesAnalysis: "Your personalised tiesAnalysis is being prepared — please refresh in a moment.",
        ukDayCount: "Your personalised ukDayCount is being prepared — please refresh in a moment.",
        worldwideIncomeObligation: "Your personalised worldwideIncomeObligation is being prepared — please refresh in a moment.",
        filingRequirements: "Your personalised filingRequirements is being prepared — please refresh in a moment.",
        splitYearEligibility: "Your personalised splitYearEligibility is being prepared — please refresh in a moment.",
        immediateActions: "Your personalised immediateActions is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact HM Revenue & Customs (HMRC) position based on my answers?",
          "What is the single most important action I should take before 31 January 2027?",
          "Are there any planning opportunities specific to my situation?",
        ],
        
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const uk_days = sessionStorage.getItem("uk-residency_uk_days") || "46_to_90";
    const previously_uk_resident = sessionStorage.getItem("uk-residency_previously_uk_resident") || "yes";
    const ties_count = sessionStorage.getItem("uk-residency_ties_count") || "3";
    const ties_detail = sessionStorage.getItem("uk-residency_ties_detail") || "family_accommodation_work";
    const aot_applies = sessionStorage.getItem("uk-residency_aot_applies") || "no";
    const art_applies = sessionStorage.getItem("uk-residency_art_applies") || "no";
    const residency_outcome = sessionStorage.getItem("uk-residency_residency_outcome") || "UK RESIDENT";
    const status = sessionStorage.getItem("uk-residency_status") || "UK RESIDENT — SUFFICIENT TIES";
    const tier = sessionStorage.getItem("uk-residency_tier") || "147";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:UK Statutory Residence Test Auditor — Deadlines`,
      "BEGIN:VEVENT",
      `UID:ukr-oct-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20261031"}`,
      `DTEND;VALUE=DATE:${"20261031"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:UK Self Assessment — paper deadline 31 October",
      "DESCRIPTION:Paper return filing deadline for previous tax year.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:ukr-jan-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270131"}`,
      `DTEND;VALUE=DATE:${"20270131"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:UK Self Assessment — online deadline 31 January",
      "DESCRIPTION:Online filing + tax payment deadline for previous tax year (2025/26).",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:ukr-july-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20260731"}`,
      `DTEND;VALUE=DATE:${"20260731"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:UK payment on account — 31 July",
      "DESCRIPTION:Second payment on account (if applicable) for current tax year.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "uk-residency.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your UK Residency Decision Pack — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your UK Residency Decision Pack · £67
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your UK Residency Decision Pack
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your personalised assessment — built around your exact answers, not a generic guide.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 31 January 2027</span>
            <span className="font-mono text-sm font-bold text-white">31 Jan 2027</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against HM Revenue & Customs (HMRC) rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your United Kingdom HM Revenue & Customs (HMRC) position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["srtOutcome","automaticTestApplied","tiesAnalysis","ukDayCount","worldwideIncomeObligation","filingRequirements"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* FIRST ACTION — tier 1 */}
            {assessment.firstAction && (
              <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your first action
                </p>
                <p className="text-lg font-bold leading-relaxed text-white">
                  {String(assessment.firstAction)}
                </p>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">UK Self Assessment — paper deadline 31 October</p>
                    <p className="text-xs text-neutral-500">Paper return filing deadline for previous tax year.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    31 Oct 2026
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">UK Self Assessment — online deadline 31 January</p>
                    <p className="text-xs text-neutral-500">Online filing + tax payment deadline for previous tax year (2025/26).</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    31 Jan 2027
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">UK payment on account — 31 July</p>
                    <p className="text-xs text-neutral-500">Second payment on account (if applicable) for current tax year.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    31 Jul 2026
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your five personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific HM Revenue & Customs (HMRC) position.
                Start with File 02 — it has your exact numbers.
                
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/global/uk-residency/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                
                {daysToDeadline} days to 31 January 2027.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>


            {/* UPGRADE */}
            <div className="no-print rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Want the full implementation plan?</p>
              <p className="mb-1 font-serif text-lg font-bold text-neutral-950">Your UK Residency Strategy System</p>
              <p className="mb-3 text-sm text-neutral-600">Full UK residency strategy including tie reduction plan, split-year treatment analysis, dual-residency treaty tie-breaker, UK-source income management, and audit-ready documentation.</p>
              <Link href="/nomad/check/uk-residency"
                className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950 transition">
                Upgrade — £147 →
              </Link>
            </div>

            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Start with the 183-day rule reality check</p>
              <p className="mb-2 text-xs text-neutral-600">If you have not yet classified your day count + ties at a basic level, start with the 183-Day Rule Reality Check to get oriented — then run the full SRT here.</p>
              <Link href="/nomad/check/183-day-rule" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                183-Day Rule Reality Check →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified United Kingdom tax adviser before making financial decisions.
            Based on HM Revenue & Customs (HMRC) guidance April 2026.{" "}
            <a href="https://www.gov.uk/government/publications/rdr3-statutory-residence-test-srt" target="_blank" rel="noopener noreferrer" className="underline">HMRC — Statutory Residence Test (RDR3 guidance)</a> · <a href="https://www.gov.uk/hmrc-internal-manuals/residence-domicile-and-remittance-basis" target="_blank" rel="noopener noreferrer" className="underline">HMRC Internal Manual — Residence, Domicile and Remittance Basis</a>
          </p>
        </div>

      </main>
    </div>
  );
}
