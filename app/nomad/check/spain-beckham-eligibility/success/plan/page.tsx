"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: spain-beckham · Tier 2 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "beck-01",
    "name": "Your Beckham Eligibility Assessment",
    "desc": "Your specific eligibility across the four core Beckham conditions.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "beck-02",
    "name": "Qualifying Structure Checklist",
    "desc": "What counts as a qualifying work arrangement — and what does not.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "beck-03",
    "name": "Prior Residency History Check",
    "desc": "How to establish clean 5-year prior residency and document it.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "beck-04",
    "name": "Modelo 149 Application Timeline",
    "desc": "The 6-month clock: when it starts, what triggers it, how to apply.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "beck-05",
    "name": "Your Adviser Brief — Beckham Application",
    "desc": "Questions for your Spanish gestor / tax adviser before application.",
    "tier": 1
  },
  {
    "num": "06",
    "slug": "beck-06",
    "name": "Employment Structure Optimisation Plan",
    "desc": "How to restructure your arrangement if current setup does not qualify.",
    "tier": 2
  },
  {
    "num": "07",
    "slug": "beck-07",
    "name": "A1 Certificate and Social Security Strategy",
    "desc": "Detailed A1 process for EU/EEA posted workers + alternative SS routes.",
    "tier": 2
  },
  {
    "num": "08",
    "slug": "beck-08",
    "name": "Full Beckham Application Roadmap",
    "desc": "Step-by-step 6-month roadmap from arrival to AEAT acknowledgment.",
    "tier": 2
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessPlan() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2027-06-30T23:59:59.000+02:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const move_reason = sessionStorage.getItem("spain-beckham_move_reason") || "employment";
      const prior_residency = sessionStorage.getItem("spain-beckham_prior_residency") || "no";
      const ss_coverage = sessionStorage.getItem("spain-beckham_ss_coverage") || "registering_spain";
      const director_ownership = sessionStorage.getItem("spain-beckham_director_ownership") || "not_applicable";
      const dnv_status = sessionStorage.getItem("spain-beckham_dnv_status") || "not_applicable";
      const application_timing = sessionStorage.getItem("spain-beckham_application_timing") || "within_6_months";
      const eligibility_outcome = sessionStorage.getItem("spain-beckham_eligibility_outcome") || "LIKELY ELIGIBLE";
      const annual_saving = sessionStorage.getItem("spain-beckham_annual_saving") || "11200";
      const status = sessionStorage.getItem("spain-beckham_status") || "LIKELY ELIGIBLE — CONFIRM STRUCTURE";
      const tier = sessionStorage.getItem("spain-beckham_tier") || "67";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "move_reason": move_reason,
        "prior_residency": prior_residency,
        "ss_coverage": ss_coverage,
        "director_ownership": director_ownership,
        "dnv_status": dnv_status,
        "application_timing": application_timing,
        "eligibility_outcome": eligibility_outcome,
        "annual_saving": annual_saving,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "employment");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "spain-beckham",
          market:     "Spain",
          authority:  "Agencia Estatal de Administración Tributaria (AEAT)",
          tier:       2,
          name,
          inputs: {
        "Reason for moving to Spain": move_reason,
        "Prior Spain residency": prior_residency,
        "Social security coverage": ss_coverage,
        "Director ownership stake": director_ownership,
        "Digital nomad visa status": dnv_status,
        "Application timing": application_timing,
        "Eligibility assessment": eligibility_outcome,
        "Estimated annual saving": annual_saving,
        "Verdict status": status,
        "Tier purchased": tier,
          },
          fields: ["beckhamEligibilityAssessment","employmentStructureAnalysis","priorResidencyStatus","socialSecurityPosition","applicationTimingStatus","estimatedTaxSaving","keyFailureRisks","structureFixRequired","immediateActions","employmentRestructuringPlan","a1CertificateStrategy","modelo149ApplicationRoadmap","startupCertificationPathway","fullApprovalTimeline"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        beckhamEligibilityAssessment: "Your personalised beckhamEligibilityAssessment is being prepared — please refresh in a moment.",
        employmentStructureAnalysis: "Your personalised employmentStructureAnalysis is being prepared — please refresh in a moment.",
        priorResidencyStatus: "Your personalised priorResidencyStatus is being prepared — please refresh in a moment.",
        socialSecurityPosition: "Your personalised socialSecurityPosition is being prepared — please refresh in a moment.",
        applicationTimingStatus: "Your personalised applicationTimingStatus is being prepared — please refresh in a moment.",
        estimatedTaxSaving: "Your personalised estimatedTaxSaving is being prepared — please refresh in a moment.",
        keyFailureRisks: "Your personalised keyFailureRisks is being prepared — please refresh in a moment.",
        structureFixRequired: "Your personalised structureFixRequired is being prepared — please refresh in a moment.",
        immediateActions: "Your personalised immediateActions is being prepared — please refresh in a moment.",
        employmentRestructuringPlan: "Your personalised employmentRestructuringPlan is being prepared — please refresh in a moment.",
        a1CertificateStrategy: "Your personalised a1CertificateStrategy is being prepared — please refresh in a moment.",
        modelo149ApplicationRoadmap: "Your personalised modelo149ApplicationRoadmap is being prepared — please refresh in a moment.",
        startupCertificationPathway: "Your personalised startupCertificationPathway is being prepared — please refresh in a moment.",
        fullApprovalTimeline: "Your personalised fullApprovalTimeline is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact Agencia Estatal de Administración Tributaria (AEAT) position based on my answers?",
          "What is the single most important action I should take before 30 June 2027?",
          "Are there any planning opportunities specific to my situation?",
        ],
        actions: [],
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const move_reason = sessionStorage.getItem("spain-beckham_move_reason") || "employment";
    const prior_residency = sessionStorage.getItem("spain-beckham_prior_residency") || "no";
    const ss_coverage = sessionStorage.getItem("spain-beckham_ss_coverage") || "registering_spain";
    const director_ownership = sessionStorage.getItem("spain-beckham_director_ownership") || "not_applicable";
    const dnv_status = sessionStorage.getItem("spain-beckham_dnv_status") || "not_applicable";
    const application_timing = sessionStorage.getItem("spain-beckham_application_timing") || "within_6_months";
    const eligibility_outcome = sessionStorage.getItem("spain-beckham_eligibility_outcome") || "LIKELY ELIGIBLE";
    const annual_saving = sessionStorage.getItem("spain-beckham_annual_saving") || "11200";
    const status = sessionStorage.getItem("spain-beckham_status") || "LIKELY ELIGIBLE — CONFIRM STRUCTURE";
    const tier = sessionStorage.getItem("spain-beckham_tier") || "67";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:Spain Beckham Eligibility Wall — Deadlines`,
      "BEGIN:VEVENT",
      `UID:beck-review-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(90)}`,
      `DTEND;VALUE=DATE:${relativeDate(90)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Beckham — 90-day strategic review",
      "DESCRIPTION:Review structure + prior residency + A1/SS + Modelo 149 timing.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:beck-modelo-149-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(180)}`,
      `DTEND;VALUE=DATE:${relativeDate(180)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Modelo 149 — absolute deadline",
      "DESCRIPTION:Submit within 6 months of Spanish SS registration.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:beck-irpf-2027-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270630"}`,
      `DTEND;VALUE=DATE:${"20270630"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Spanish IRPF return — 30 June 2027",
      "DESCRIPTION:First return at Beckham rate.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:beck-irpf-2028-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20280630"}`,
      `DTEND;VALUE=DATE:${"20280630"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Spanish IRPF return — 30 June 2028",
      "DESCRIPTION:Year 2 of 6-year regime.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:beck-regime-end-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(2190)}`,
      `DTEND;VALUE=DATE:${relativeDate(2190)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Beckham regime end — tax year 6",
      "DESCRIPTION:Regime ends 5 full tax years after arrival year.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "spain-beckham.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your Beckham Approval System — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your Beckham Approval System · £147
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your Beckham Approval System
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your full implementation plan — built around your specific inputs, not the average taxpayer.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 30 June 2027</span>
            <span className="font-mono text-sm font-bold text-white">30 Jun 2027</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against Agencia Estatal de Administración Tributaria (AEAT) rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your Spain Agencia Estatal de Administración Tributaria (AEAT) position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["beckhamEligibilityAssessment","employmentStructureAnalysis","priorResidencyStatus","socialSecurityPosition","applicationTimingStatus","estimatedTaxSaving"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* ACTIONS CHECKLIST — tier 2 */}
            {Array.isArray(assessment.actions) && assessment.actions.length > 0 && (
              <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your action checklist
                </p>
                <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                  What to do — in order — before 30 June 2027
                </h2>
                <div className="space-y-4">
                  {(assessment.actions as Action[]).map((action, i) => (
                    <div key={i} className={`rounded-xl border p-5 transition ${checked[i] ? "border-emerald-200 bg-emerald-50" : "border-neutral-200 bg-neutral-50"}`}>
                      <div className="flex items-start gap-3 mb-3">
                        <button onClick={() => setChecked(p => ({ ...p, [i]: !p[i] }))}
                          className={`no-print mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 transition ${checked[i] ? "border-emerald-500 bg-emerald-500" : "border-neutral-300 bg-white hover:border-neutral-950"}`}>
                          {checked[i] && <span className="text-xs font-bold text-white">✓</span>}
                        </button>
                        <div className="flex-1">
                          <div className="flex items-start justify-between gap-2">
                            <p className={`font-bold ${checked[i] ? "text-neutral-400 line-through" : "text-neutral-950"}`}>
                              {i + 1}. {action.title}
                            </p>
                            <span className="shrink-0 rounded-lg bg-red-100 px-2 py-0.5 font-mono text-[10px] font-bold text-red-700">
                              {action.deadline}
                            </span>
                          </div>
                        </div>
                      </div>
                      <div className="ml-9 space-y-2">
                        {action.steps?.map((step, j) => (
                          <div key={j} className="flex items-start gap-2">
                            <span className="mt-0.5 shrink-0 font-mono text-xs text-neutral-400">{j+1}.</span>
                            <p className="text-sm leading-relaxed text-neutral-700">{step}</p>
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Beckham — 90-day strategic review</p>
                    <p className="text-xs text-neutral-500">Review structure + prior residency + A1/SS + Modelo 149 timing.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Modelo 149 — absolute deadline</p>
                    <p className="text-xs text-neutral-500">Submit within 6 months of Spanish SS registration.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Spanish IRPF return — 30 June 2027</p>
                    <p className="text-xs text-neutral-500">First return at Beckham rate.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    30 Jun 2027
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Spanish IRPF return — 30 June 2028</p>
                    <p className="text-xs text-neutral-500">Year 2 of 6-year regime.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    30 Jun 2028
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Beckham regime end — tax year 6</p>
                    <p className="text-xs text-neutral-500">Regime ends 5 full tax years after arrival year.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your eight personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific Agencia Estatal de Administración Tributaria (AEAT) position.
                Start with File 02 — it has your exact numbers.
                Files 06–08 are exclusive to this plan.
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/global/spain-beckham/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                Work through the checklist above.
                {daysToDeadline} days to 30 June 2027.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>



            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Confirm your departure country residency first</p>
              <p className="mb-2 text-xs text-neutral-600">Eligibility depends on when you cease tax residency in your home country. Use the 183-Day Rule Reality Check to establish your position in your current country, then return here for the Beckham-specific analysis.</p>
              <Link href="/nomad/check/183-day-rule" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                Residency Reality Check →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified Spain tax adviser before making financial decisions.
            Based on Agencia Estatal de Administración Tributaria (AEAT) guidance April 2026.{" "}
            <a href="https://sede.agenciatributaria.gob.es/Sede/procedimientoini/GI24.shtml" target="_blank" rel="noopener noreferrer" className="underline">AEAT — Special Expat Regime (Modelo 149)</a> · <a href="https://www.boe.es/buscar/act.php?id=BOE-A-2006-20764" target="_blank" rel="noopener noreferrer" className="underline">Ley 35/2006 (IRPF Act) Art. 93</a>
          </p>
        </div>

      </main>
    </div>
  );
}
