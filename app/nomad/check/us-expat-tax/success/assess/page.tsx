"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: us-expat-tax · Tier 1 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "uset-01",
    "name": "Your FEIE vs FTC Comparison",
    "desc": "Side-by-side numeric comparison of FEIE-only, FTC-only, and hybrid outcomes for your specific situation.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "uset-02",
    "name": "Optimal Strategy Determination",
    "desc": "Recommended approach for your country, income type, and income level — with reasoning.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "uset-03",
    "name": "Filing Requirements Checklist",
    "desc": "All the US federal filings a US expat must complete — forms, deadlines, and extensions.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "uset-04",
    "name": "FBAR and FATCA Obligations",
    "desc": "Detailed FBAR + Form 8938 compliance — thresholds, deadlines, penalties, accounts covered.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "uset-05",
    "name": "Your Accountant Brief — US Expat Strategy",
    "desc": "Questions for your US expat tax specialist — ideally EA or CPA with cross-border experience.",
    "tier": 1
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessAssess() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2027-06-15T23:59:59.000-04:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const residence_country = sessionStorage.getItem("us-expat-tax_residence_country") || "UK";
      const income_type = sessionStorage.getItem("us-expat-tax_income_type") || "employment";
      const annual_income = sessionStorage.getItem("us-expat-tax_annual_income") || "126500_to_200k";
      const foreign_tax_rate = sessionStorage.getItem("us-expat-tax_foreign_tax_rate") || "over_35";
      const current_method = sessionStorage.getItem("us-expat-tax_current_method") || "feie";
      const has_passive_income = sessionStorage.getItem("us-expat-tax_has_passive_income") || "no";
      const days_abroad = sessionStorage.getItem("us-expat-tax_days_abroad") || "330_plus";
      const recommended_strategy = sessionStorage.getItem("us-expat-tax_recommended_strategy") || "FTC";
      const annual_saving = sessionStorage.getItem("us-expat-tax_annual_saving") || "2800";
      const status = sessionStorage.getItem("us-expat-tax_status") || "FTC RECOMMENDED — FEIE WASTING CREDITS";
      const tier = sessionStorage.getItem("us-expat-tax_tier") || "147";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "residence_country": residence_country,
        "income_type": income_type,
        "annual_income": annual_income,
        "foreign_tax_rate": foreign_tax_rate,
        "current_method": current_method,
        "has_passive_income": has_passive_income,
        "days_abroad": days_abroad,
        "recommended_strategy": recommended_strategy,
        "annual_saving": annual_saving,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "UK");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "us-expat-tax",
          market:     "United States",
          authority:  "Internal Revenue Service (IRS)",
          tier:       1,
          name,
          inputs: {
        "Country of residence": residence_country,
        "Primary income type": income_type,
        "Annual income band": annual_income,
        "Foreign effective tax rate": foreign_tax_rate,
        "Current method": current_method,
        "Has passive income": has_passive_income,
        "Days outside US": days_abroad,
        "Recommended strategy": recommended_strategy,
        "Annual saving from switch": annual_saving,
        "Verdict status": status,
        "Tier purchased": tier,
          },
          fields: ["currentStrategyAnalysis","feieEligibility","ftcCalculation","optimalStrategyRecommendation","annualTaxSaving","passiveIncomeStrategy","filingObligations","fbarFatcaRequirements","immediateActions"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        currentStrategyAnalysis: "Your personalised currentStrategyAnalysis is being prepared — please refresh in a moment.",
        feieEligibility: "Your personalised feieEligibility is being prepared — please refresh in a moment.",
        ftcCalculation: "Your personalised ftcCalculation is being prepared — please refresh in a moment.",
        optimalStrategyRecommendation: "Your personalised optimalStrategyRecommendation is being prepared — please refresh in a moment.",
        annualTaxSaving: "Your personalised annualTaxSaving is being prepared — please refresh in a moment.",
        passiveIncomeStrategy: "Your personalised passiveIncomeStrategy is being prepared — please refresh in a moment.",
        filingObligations: "Your personalised filingObligations is being prepared — please refresh in a moment.",
        fbarFatcaRequirements: "Your personalised fbarFatcaRequirements is being prepared — please refresh in a moment.",
        immediateActions: "Your personalised immediateActions is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact Internal Revenue Service (IRS) position based on my answers?",
          "What is the single most important action I should take before 15 June 2027?",
          "Are there any planning opportunities specific to my situation?",
        ],
        
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const residence_country = sessionStorage.getItem("us-expat-tax_residence_country") || "UK";
    const income_type = sessionStorage.getItem("us-expat-tax_income_type") || "employment";
    const annual_income = sessionStorage.getItem("us-expat-tax_annual_income") || "126500_to_200k";
    const foreign_tax_rate = sessionStorage.getItem("us-expat-tax_foreign_tax_rate") || "over_35";
    const current_method = sessionStorage.getItem("us-expat-tax_current_method") || "feie";
    const has_passive_income = sessionStorage.getItem("us-expat-tax_has_passive_income") || "no";
    const days_abroad = sessionStorage.getItem("us-expat-tax_days_abroad") || "330_plus";
    const recommended_strategy = sessionStorage.getItem("us-expat-tax_recommended_strategy") || "FTC";
    const annual_saving = sessionStorage.getItem("us-expat-tax_annual_saving") || "2800";
    const status = sessionStorage.getItem("us-expat-tax_status") || "FTC RECOMMENDED — FEIE WASTING CREDITS";
    const tier = sessionStorage.getItem("us-expat-tax_tier") || "147";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:US Citizen Abroad Optimizer — Deadlines`,
      "BEGIN:VEVENT",
      `UID:uset-apr-15-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270415"}`,
      `DTEND;VALUE=DATE:${"20270415"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:US Form 1040 — 15 April 2027 (standard)",
      "DESCRIPTION:Standard filing deadline; payment due regardless of extension.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:uset-jun-15-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270615"}`,
      `DTEND;VALUE=DATE:${"20270615"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:US Form 1040 — 15 June 2027 (auto extension for expats)",
      "DESCRIPTION:Automatic 2-month extension for US citizens abroad.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:uset-oct-15-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20271015"}`,
      `DTEND;VALUE=DATE:${"20271015"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:US Form 1040 + FBAR — 15 October 2027 (final extension)",
      "DESCRIPTION:Further extension to 15 October via Form 4868 / automatic for FBAR.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "us-expat-tax.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your US Expat Tax Strategy Report — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your US Expat Tax Strategy Report · $67
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your US Expat Tax Strategy Report
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your personalised assessment — built around your exact answers, not a generic guide.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 15 June 2027</span>
            <span className="font-mono text-sm font-bold text-white">15 Jun 2027</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against Internal Revenue Service (IRS) rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your United States Internal Revenue Service (IRS) position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["currentStrategyAnalysis","feieEligibility","ftcCalculation","optimalStrategyRecommendation","annualTaxSaving","passiveIncomeStrategy"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* FIRST ACTION — tier 1 */}
            {assessment.firstAction && (
              <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your first action
                </p>
                <p className="text-lg font-bold leading-relaxed text-white">
                  {String(assessment.firstAction)}
                </p>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">US Form 1040 — 15 April 2027 (standard)</p>
                    <p className="text-xs text-neutral-500">Standard filing deadline; payment due regardless of extension.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    15 Apr 2027
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">US Form 1040 — 15 June 2027 (auto extension for expats)</p>
                    <p className="text-xs text-neutral-500">Automatic 2-month extension for US citizens abroad.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    15 Jun 2027
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">US Form 1040 + FBAR — 15 October 2027 (final extension)</p>
                    <p className="text-xs text-neutral-500">Further extension to 15 October via Form 4868 / automatic for FBAR.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    15 Oct 2027
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your five personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific Internal Revenue Service (IRS) position.
                Start with File 02 — it has your exact numbers.
                
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/global/us-expat-tax/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                
                {daysToDeadline} days to 15 June 2027.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>


            {/* UPGRADE */}
            <div className="no-print rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Want the full implementation plan?</p>
              <p className="mb-1 font-serif text-lg font-bold text-neutral-950">Your Global Tax Optimization System</p>
              <p className="mb-3 text-sm text-neutral-600">Complete US expat tax strategy: hybrid FEIE/FTC optimisation, multi-year FTC carryforward planning, income classification strategy, cross-border structuring, and audit-ready documentation.</p>
              <Link href="/nomad/check/us-expat-tax"
                className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950 transition">
                Upgrade — $147 →
              </Link>
            </div>

            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Confirm your FEIE eligibility first</p>
              <p className="mb-2 text-xs text-neutral-600">FEIE requires the physical presence test (330 days) or bona fide residence test. If you are not clearly qualifying under either, FTC is your only option. Use the FEIE Nomad Auditor to confirm eligibility, then return here for the strategy comparison.</p>
              <Link href="/us/check/feie-nomad-auditor" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                FEIE Nomad Auditor →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified United States tax adviser before making financial decisions.
            Based on Internal Revenue Service (IRS) guidance April 2026.{" "}
            <a href="https://www.irs.gov/individuals/international-taxpayers/foreign-earned-income-exclusion" target="_blank" rel="noopener noreferrer" className="underline">IRS — Foreign Earned Income Exclusion</a> · <a href="https://www.irs.gov/individuals/international-taxpayers/foreign-tax-credit" target="_blank" rel="noopener noreferrer" className="underline">IRS — Foreign Tax Credit</a>
          </p>
        </div>

      </main>
    </div>
  );
}
