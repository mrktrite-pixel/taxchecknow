"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: pension-iht-trap · Tier 2 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "piht-01",
    "name": "Your Exposure Report — Current vs Proposed",
    "desc": "Side-by-side IHT calculation under current law and the October 2024 proposal.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "piht-02",
    "name": "Beneficiary Nomination Review",
    "desc": "How beneficiary designation affects IHT treatment under the proposal.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "piht-03",
    "name": "Planning Options Ranked",
    "desc": "Drawdown, gifting, insurance, rebalancing — what to do before April 2027.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "piht-04",
    "name": "Timing Before Royal Assent",
    "desc": "What to do now vs what to defer — given the proposal is not yet law.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "piht-05",
    "name": "IFA / Estate Planner Brief",
    "desc": "5 questions to ask your financial adviser or estate planner.",
    "tier": 1
  },
  {
    "num": "06",
    "slug": "piht-06",
    "name": "Drawdown Strategy Playbook",
    "desc": "Full drawdown strategy for reducing pension value before April 2027.",
    "tier": 2
  },
  {
    "num": "07",
    "slug": "piht-07",
    "name": "Whole-of-Life Insurance in Trust",
    "desc": "How to use a WoL policy to cover IHT on pension — full execution guide.",
    "tier": 2
  },
  {
    "num": "08",
    "slug": "piht-08",
    "name": "Multi-Scenario Planning Timeline",
    "desc": "Action plan that works across enactment, delay, and amendment scenarios.",
    "tier": 2
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessPlan() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2027-04-06T00:00:00.000+01:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const pension_value = sessionStorage.getItem("pension-iht-trap_pension_value") || "325k_to_500k";
      const estate_value = sessionStorage.getItem("pension-iht-trap_estate_value") || "500k_to_1m";
      const marital_status = sessionStorage.getItem("pension-iht-trap_marital_status") || "married";
      const beneficiaries = sessionStorage.getItem("pension-iht-trap_beneficiaries") || "children";
      const exposure_status = sessionStorage.getItem("pension-iht-trap_exposure_status") || "PROPOSED_IHT_EXPOSURE";
      const total_estate = sessionStorage.getItem("pension-iht-trap_total_estate") || "1150000";
      const proposed_pension_iht = sessionStorage.getItem("pension-iht-trap_proposed_pension_iht") || "160000";
      const combined_pension_cost = sessionStorage.getItem("pension-iht-trap_combined_pension_cost") || "276000";
      const status = sessionStorage.getItem("pension-iht-trap_status") || "PROPOSED IHT EXPOSURE";
      const tier = sessionStorage.getItem("pension-iht-trap_tier") || "147";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "pension_value": pension_value,
        "estate_value": estate_value,
        "marital_status": marital_status,
        "beneficiaries": beneficiaries,
        "exposure_status": exposure_status,
        "total_estate": total_estate,
        "proposed_pension_iht": proposed_pension_iht,
        "combined_pension_cost": combined_pension_cost,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "325k_to_500k");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "pension-iht-trap",
          market:     "United Kingdom",
          authority:  "HMRC",
          tier:       2,
          name,
          inputs: {
        "DC pension value band": pension_value,
        "Estate value excl. pension": estate_value,
        "Marital status": marital_status,
        "Pension beneficiaries": beneficiaries,
        "Exposure classification": exposure_status,
        "Combined estate": total_estate,
        "Proposed pension IHT (if enacted)": proposed_pension_iht,
        "Combined IHT + income tax on pension": combined_pension_cost,
        "Verdict status": status,
        "Product tier purchased": tier,
          },
          fields: ["exposureStatusSummary","currentVsProposedComparison","beneficiaryNominationReview","drawdownStrategyPlan","lifetimeGiftingStrategy","wholeOfLifeInsuranceSizing","isaVsPensionRebalancing","spouseEqualisationPlan","multiScenarioTimeline","ifaCoordinationBrief"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        exposureStatusSummary: "Your personalised exposureStatusSummary is being prepared — please refresh in a moment.",
        currentVsProposedComparison: "Your personalised currentVsProposedComparison is being prepared — please refresh in a moment.",
        beneficiaryNominationReview: "Your personalised beneficiaryNominationReview is being prepared — please refresh in a moment.",
        drawdownStrategyPlan: "Your personalised drawdownStrategyPlan is being prepared — please refresh in a moment.",
        lifetimeGiftingStrategy: "Your personalised lifetimeGiftingStrategy is being prepared — please refresh in a moment.",
        wholeOfLifeInsuranceSizing: "Your personalised wholeOfLifeInsuranceSizing is being prepared — please refresh in a moment.",
        isaVsPensionRebalancing: "Your personalised isaVsPensionRebalancing is being prepared — please refresh in a moment.",
        spouseEqualisationPlan: "Your personalised spouseEqualisationPlan is being prepared — please refresh in a moment.",
        multiScenarioTimeline: "Your personalised multiScenarioTimeline is being prepared — please refresh in a moment.",
        ifaCoordinationBrief: "Your personalised ifaCoordinationBrief is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact HMRC position based on my answers?",
          "What is the single most important action I should take before 6 April 2027?",
          "Are there any planning opportunities specific to my situation?",
        ],
        actions: [],
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const pension_value = sessionStorage.getItem("pension-iht-trap_pension_value") || "325k_to_500k";
    const estate_value = sessionStorage.getItem("pension-iht-trap_estate_value") || "500k_to_1m";
    const marital_status = sessionStorage.getItem("pension-iht-trap_marital_status") || "married";
    const beneficiaries = sessionStorage.getItem("pension-iht-trap_beneficiaries") || "children";
    const exposure_status = sessionStorage.getItem("pension-iht-trap_exposure_status") || "PROPOSED_IHT_EXPOSURE";
    const total_estate = sessionStorage.getItem("pension-iht-trap_total_estate") || "1150000";
    const proposed_pension_iht = sessionStorage.getItem("pension-iht-trap_proposed_pension_iht") || "160000";
    const combined_pension_cost = sessionStorage.getItem("pension-iht-trap_combined_pension_cost") || "276000";
    const status = sessionStorage.getItem("pension-iht-trap_status") || "PROPOSED IHT EXPOSURE";
    const tier = sessionStorage.getItem("pension-iht-trap_tier") || "147";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:Pension IHT Trap 2027 Engine — Deadlines`,
      "BEGIN:VEVENT",
      `UID:piht-review-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(7)}`,
      `DTEND;VALUE=DATE:${relativeDate(7)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Beneficiary nomination review",
      "DESCRIPTION:Confirm expression of wishes matches current intent.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:piht-insurance-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(30)}`,
      `DTEND;VALUE=DATE:${relativeDate(30)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Whole-of-life insurance quotes",
      "DESCRIPTION:Obtain 3 quotes while still in reasonable health.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:piht-drawdown-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(60)}`,
      `DTEND;VALUE=DATE:${relativeDate(60)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Drawdown strategy kick-off",
      "DESCRIPTION:First planned drawdown if selected as part of strategy.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:piht-budget-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20261031"}`,
      `DTEND;VALUE=DATE:${"20261031"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Budget announcement watch",
      "DESCRIPTION:Check for proposal progress or amendments.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:piht-proposed-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270406"}`,
      `DTEND;VALUE=DATE:${"20270406"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Proposed effective date (if enacted)",
      "DESCRIPTION:6 April 2027 — plan must be complete by this date if enacted.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "pension-iht-trap.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your Pension IHT Strategy Pack — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your Pension IHT Strategy Pack · £147
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your Pension IHT Strategy Pack
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your full implementation plan — built around your specific inputs, not the average taxpayer.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 6 April 2027</span>
            <span className="font-mono text-sm font-bold text-white">6 Apr 2027</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against HMRC rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your United Kingdom HMRC position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["exposureStatusSummary","currentVsProposedComparison","beneficiaryNominationReview","drawdownStrategyPlan","lifetimeGiftingStrategy","wholeOfLifeInsuranceSizing"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* ACTIONS CHECKLIST — tier 2 */}
            {Array.isArray(assessment.actions) && assessment.actions.length > 0 && (
              <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your action checklist
                </p>
                <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                  What to do — in order — before 6 April 2027
                </h2>
                <div className="space-y-4">
                  {(assessment.actions as Action[]).map((action, i) => (
                    <div key={i} className={`rounded-xl border p-5 transition ${checked[i] ? "border-emerald-200 bg-emerald-50" : "border-neutral-200 bg-neutral-50"}`}>
                      <div className="flex items-start gap-3 mb-3">
                        <button onClick={() => setChecked(p => ({ ...p, [i]: !p[i] }))}
                          className={`no-print mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 transition ${checked[i] ? "border-emerald-500 bg-emerald-500" : "border-neutral-300 bg-white hover:border-neutral-950"}`}>
                          {checked[i] && <span className="text-xs font-bold text-white">✓</span>}
                        </button>
                        <div className="flex-1">
                          <div className="flex items-start justify-between gap-2">
                            <p className={`font-bold ${checked[i] ? "text-neutral-400 line-through" : "text-neutral-950"}`}>
                              {i + 1}. {action.title}
                            </p>
                            <span className="shrink-0 rounded-lg bg-red-100 px-2 py-0.5 font-mono text-[10px] font-bold text-red-700">
                              {action.deadline}
                            </span>
                          </div>
                        </div>
                      </div>
                      <div className="ml-9 space-y-2">
                        {action.steps?.map((step, j) => (
                          <div key={j} className="flex items-start gap-2">
                            <span className="mt-0.5 shrink-0 font-mono text-xs text-neutral-400">{j+1}.</span>
                            <p className="text-sm leading-relaxed text-neutral-700">{step}</p>
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Beneficiary nomination review</p>
                    <p className="text-xs text-neutral-500">Confirm expression of wishes matches current intent.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Whole-of-life insurance quotes</p>
                    <p className="text-xs text-neutral-500">Obtain 3 quotes while still in reasonable health.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Drawdown strategy kick-off</p>
                    <p className="text-xs text-neutral-500">First planned drawdown if selected as part of strategy.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    This week
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Budget announcement watch</p>
                    <p className="text-xs text-neutral-500">Check for proposal progress or amendments.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    31 Oct 2026
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Proposed effective date (if enacted)</p>
                    <p className="text-xs text-neutral-500">6 April 2027 — plan must be complete by this date if enacted.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    6 Apr 2027
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your eight personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific HMRC position.
                Start with File 02 — it has your exact numbers.
                Files 06–08 are exclusive to this plan.
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/uk/pension-iht-trap/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                Work through the checklist above.
                {daysToDeadline} days to 6 April 2027.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>



            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Higher earner? Also check the 60% tax trap.</p>
              <p className="mb-2 text-xs text-neutral-600">If your total income (including pension drawdowns) is above £100,000, the personal allowance tapers at 60% effective rate between £100,000 and £125,140. Our 60% Tax Trap Engine shows the exposure and pension-based escape.</p>
              <Link href="/uk/check/allowance-sniper" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                Check the 60% trap →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified United Kingdom tax adviser before making financial decisions.
            Based on HMRC guidance April 2026.{" "}
            <a href="https://www.gov.uk/government/consultations/inheritance-tax-on-pensions-liability-reporting-and-payment" target="_blank" rel="noopener noreferrer" className="underline">HM Treasury — 'Inheritance Tax — pensions' consultation (October 2024)</a> · <a href="https://www.gov.uk/inheritance-tax" target="_blank" rel="noopener noreferrer" className="underline">HMRC — Inheritance Tax overview</a>
          </p>
        </div>

      </main>
    </div>
  );
}
