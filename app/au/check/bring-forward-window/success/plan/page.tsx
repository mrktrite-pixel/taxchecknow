"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: bring-forward-window · Tier 2 Success Page
import { useEffect, useState } from "react";
import Link from "next/link";

const FILES = [
  {
    "num": "01",
    "slug": "bfw-01",
    "name": "Your 30 June Sequencing Verdict",
    "desc": "Your exact contribution capacity across 30 June and 1 July — sequenced or not.",
    "tier": 1
  },
  {
    "num": "02",
    "slug": "bfw-02",
    "name": "30 June / 1 July Sequencing Plan",
    "desc": "The exact timing and amounts to maximise your bring-forward across both financial years.",
    "tier": 1
  },
  {
    "num": "03",
    "slug": "bfw-03",
    "name": "TSB Bracket Calculator",
    "desc": "The exact thresholds, amounts, and what each bracket means for your sequencing window.",
    "tier": 1
  },
  {
    "num": "04",
    "slug": "bfw-04",
    "name": "Evidence & Records Checklist",
    "desc": "Documentation needed for your SMSF trustee minutes, work test, and ATO audit defence.",
    "tier": 1
  },
  {
    "num": "05",
    "slug": "bfw-05",
    "name": "Your Accountant Brief",
    "desc": "3 questions to take to your accountant before 30 June 2026.",
    "tier": 1
  },
  {
    "num": "06",
    "slug": "bfw-06",
    "name": "Full Sequencing Execution Plan",
    "desc": "Step-by-step implementation with SMSF trustee minutes, super fund contribution forms, and timing buffers.",
    "tier": 2
  },
  {
    "num": "07",
    "slug": "bfw-07",
    "name": "SMSF & Division 296 Interaction Blueprint",
    "desc": "How to model the sequencing window together with Division 296 for balances approaching $3M.",
    "tier": 2
  },
  {
    "num": "08",
    "slug": "bfw-08",
    "name": "Long-Term Super Contribution Model",
    "desc": "Multi-year contribution strategy integrating bring-forward, carry-forward concessional, and Division 296.",
    "tier": 2
  }
];

interface Action { title: string; deadline: string; steps: string[]; }
type Assessment = Record<string, unknown> & {
  accountantQuestions?: string[];
  actions?: Action[];
};

export default function SuccessPlan() {
  const [firstName,  setFirstName]  = useState("there");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [error,      setError]      = useState("");
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number,boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2026-06-30T23:59:59.000+10:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name = "there";
    if (sessionId) {
      try {
        const r = await fetch(`/api/get-session?id=${sessionId}`);
        const d = await r.json();
        if (d.firstName) { name = d.firstName; setFirstName(d.firstName); }
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    setError("");
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");

    try {
      // ── STEP 1: Try fetching pre-generated assessment from Supabase ──
      // Generated by webhook at purchase time — instant load, no API call
      if (sessionId) {
        const r = await fetch(`/api/get-assessment?session_id=${sessionId}`);
        if (r.ok) {
          const d = await r.json();
          if (d.assessment) {
            setAssessment(d.assessment);
            setLoading(false);
            return;
          }
        }
      }

      // ── STEP 2: Fallback — generate now via /api/assess ──────────────
      // Runs if webhook hasn't stored assessment yet (e.g. timing, retry)
      const tsb_band = sessionStorage.getItem("bring-forward-window_tsb_band") || "under_1_84m";
      const age = sessionStorage.getItem("bring-forward-window_age") || "under_67";
      const bring_forward_triggered = sessionStorage.getItem("bring-forward-window_bring_forward_triggered") || "false";
      const total_possible = sessionStorage.getItem("bring-forward-window_total_possible") || "510000";
      const locked_out = sessionStorage.getItem("bring-forward-window_locked_out") || "false";
      const decision_gap = sessionStorage.getItem("bring-forward-window_decision_gap") || "150000";
      const status = sessionStorage.getItem("bring-forward-window_status") || "SEQUENCING WINDOW OPEN";
      const tier = sessionStorage.getItem("bring-forward-window_tier") || "147";

      // Check if we have any real inputs — sessionStorage may be empty after Stripe redirect
      const hasInputs = Object.values({
        "tsb_band": tsb_band,
        "age": age,
        "bring_forward_triggered": bring_forward_triggered,
        "total_possible": total_possible,
        "locked_out": locked_out,
        "decision_gap": decision_gap,
        "status": status,
        "tier": tier,
      }).some(v => v && v !== "under_1_84m");

      const res = await fetch("/api/assess", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          product_id: "bring-forward-window",
          market:     "Australia",
          authority:  "ATO",
          tier:       2,
          name,
          inputs: {
        "TSB band at 30 June 2026": tsb_band,
        "Age band": age,
        "Bring-forward already triggered": bring_forward_triggered,
        "Total contribution possible": total_possible,
        "Locked out of contributions": locked_out,
        "Decision gap vs waiting": decision_gap,
        "Sequencing window verdict": status,
        "Product tier purchased": tier,
          },
          fields: ["sequencingWindowStatus","tsbBandAnalysis","bringForwardEligibility","maxBeforeJune30","maxFromJuly1","totalPossibleContribution","decisionGapExplanation","ageWorkTestCheck","smsfTimingRequirements","division296Interaction","carryForwardConcessionalOpportunity","contributionSequencingPlan","accountantImplementationChecklist"],
        }),
      });
      const data = await res.json();
      if (!res.ok || data.error) throw new Error(data.error || "Assessment failed");
      setAssessment(data.assessment);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Failed to generate assessment");
      // Graceful fallback — page still shows files and calendar
      setAssessment({
        sequencingWindowStatus: "Your personalised sequencingWindowStatus is being prepared — please refresh in a moment.",
        tsbBandAnalysis: "Your personalised tsbBandAnalysis is being prepared — please refresh in a moment.",
        bringForwardEligibility: "Your personalised bringForwardEligibility is being prepared — please refresh in a moment.",
        maxBeforeJune30: "Your personalised maxBeforeJune30 is being prepared — please refresh in a moment.",
        maxFromJuly1: "Your personalised maxFromJuly1 is being prepared — please refresh in a moment.",
        totalPossibleContribution: "Your personalised totalPossibleContribution is being prepared — please refresh in a moment.",
        decisionGapExplanation: "Your personalised decisionGapExplanation is being prepared — please refresh in a moment.",
        ageWorkTestCheck: "Your personalised ageWorkTestCheck is being prepared — please refresh in a moment.",
        smsfTimingRequirements: "Your personalised smsfTimingRequirements is being prepared — please refresh in a moment.",
        division296Interaction: "Your personalised division296Interaction is being prepared — please refresh in a moment.",
        carryForwardConcessionalOpportunity: "Your personalised carryForwardConcessionalOpportunity is being prepared — please refresh in a moment.",
        contributionSequencingPlan: "Your personalised contributionSequencingPlan is being prepared — please refresh in a moment.",
        accountantImplementationChecklist: "Your personalised accountantImplementationChecklist is being prepared — please refresh in a moment.",
        accountantQuestions: [
          "What is my exact ATO position based on my answers?",
          "What is the single most important action I should take before 30 June 2026?",
          "Are there any planning opportunities specific to my situation?",
        ],
        actions: [],
      } as unknown as Assessment);
    } finally {
      setLoading(false);
    }
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const tsb_band = sessionStorage.getItem("bring-forward-window_tsb_band") || "under_1_84m";
    const age = sessionStorage.getItem("bring-forward-window_age") || "under_67";
    const bring_forward_triggered = sessionStorage.getItem("bring-forward-window_bring_forward_triggered") || "false";
    const total_possible = sessionStorage.getItem("bring-forward-window_total_possible") || "510000";
    const locked_out = sessionStorage.getItem("bring-forward-window_locked_out") || "false";
    const decision_gap = sessionStorage.getItem("bring-forward-window_decision_gap") || "150000";
    const status = sessionStorage.getItem("bring-forward-window_status") || "SEQUENCING WINDOW OPEN";
    const tier = sessionStorage.getItem("bring-forward-window_tier") || "147";
    function relativeDate(d: number): string {
      return new Date(Date.now() + d * 86400000).toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR","VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN","METHOD:PUBLISH",
      `X-WR-CALNAME:Super Contribution Bring-Forward Window Engine — Deadlines`,
      "BEGIN:VEVENT",
      `UID:bfw-prepare-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20260601"}`,
      `DTEND;VALUE=DATE:${"20260601"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Super — confirm TSB projection with accountant",
      "DESCRIPTION:Review projected TSB at 30 June 2026 to confirm sequencing eligibility.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:bfw-june30-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20260625"}`,
      `DTEND;VALUE=DATE:${"20260625"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Super — contribute $120,000 before 30 June",
      "DESCRIPTION:Contribute annual NCC cap under 2025-26 rules for sequencing.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:bfw-july1-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20260702"}`,
      `DTEND;VALUE=DATE:${"20260702"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Super — trigger $390,000 bring-forward from 1 July",
      "DESCRIPTION:New indexed bring-forward available from 1 July under 2026-27 rules.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = "bring-forward-window.ics";
    document.body.appendChild(a); a.click();
    document.body.removeChild(a); URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment?.accountantQuestions?.length) return;
    const text = (assessment.accountantQuestions as string[])
      .map((q,i) => `${i+1}. "${q}"`).join("\n");
    await navigator.clipboard.writeText(
      `Your June 30 Execution Plan — questions for my accountant:\n\n${text}\n\nTaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const hi = firstName !== "there" ? firstName : "there";
  const greeting = firstName !== "there" ? `${firstName}` : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`@media print { .no-print{display:none!important} body{font-size:12px;color:#000} .print-section{page-break-inside:avoid} }`}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <button onClick={() => window.print()}
            className="rounded-lg border border-neutral-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-neutral-700 hover:bg-neutral-950 hover:text-white transition">
            ⬇ Save PDF
          </button>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* ── HERO — confirmation + personal hook ── */}
        <div className="print-section rounded-2xl border-2 border-emerald-500 bg-emerald-50 px-6 py-6">
          <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
            Payment confirmed · Your June 30 Execution Plan · $147
          </p>
          <h1 className="mt-2 font-serif text-2xl font-bold text-neutral-950">
            {hi !== "there" ? `${hi}, here is your ` : "Your "}Your June 30 Execution Plan
          </h1>
          <p className="mt-1 text-sm text-emerald-800">
            This is your full implementation plan — built around your specific inputs, not the average taxpayer.
          </p>
          <div className="mt-4 flex items-center justify-between rounded-xl bg-red-700 px-4 py-2.5">
            <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 30 June 2026</span>
            <span className="font-mono text-sm font-bold text-white">30 Jun 2026</span>
          </div>
        </div>

        {/* ── LOADING ── */}
        {loading && (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-semibold text-neutral-700">Building your personalised assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Analysing your answers against ATO rules</p>
          </div>
        )}

        {/* ── ERROR ── */}
        {error && !loading && (
          <div className="rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">
            ⚠ Assessment generation issue — showing your files and calendar below.
            <button onClick={() => generateAssessment(firstName)}
              className="no-print ml-2 underline font-semibold">Try again →</button>
          </div>
        )}

        {/* ── ASSESSMENT ── */}
        {!loading && assessment && (
          <>

            {/* YOUR POSITION — key verdict fields */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your Australia ATO position
              </p>
              <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                What this means for {greeting}
              </h2>
              <div className="space-y-3">
                {(["sequencingWindowStatus","tsbBandAnalysis","bringForwardEligibility","maxBeforeJune30","maxFromJuly1","totalPossibleContribution"] as string[]).map(key => {
                  const val = assessment[key];
                  if (!val || typeof val !== "string") return null;
                  return (
                    <div key={key} className="rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-4">
                      <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                        {key.replace(/([A-Z])/g,' $1').replace(/_/g,' ').trim().replace(/^./,c=>c.toUpperCase())}
                      </p>
                      <p className="text-sm leading-relaxed text-neutral-900">{val}</p>
                    </div>
                  );
                })}
              </div>
            </div>


            {/* ACTIONS CHECKLIST — tier 2 */}
            {Array.isArray(assessment.actions) && assessment.actions.length > 0 && (
              <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
                <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                  Your action checklist
                </p>
                <h2 className="mb-4 font-serif text-xl font-bold text-neutral-950">
                  What to do — in order — before 30 June 2026
                </h2>
                <div className="space-y-4">
                  {(assessment.actions as Action[]).map((action, i) => (
                    <div key={i} className={`rounded-xl border p-5 transition ${checked[i] ? "border-emerald-200 bg-emerald-50" : "border-neutral-200 bg-neutral-50"}`}>
                      <div className="flex items-start gap-3 mb-3">
                        <button onClick={() => setChecked(p => ({ ...p, [i]: !p[i] }))}
                          className={`no-print mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 transition ${checked[i] ? "border-emerald-500 bg-emerald-500" : "border-neutral-300 bg-white hover:border-neutral-950"}`}>
                          {checked[i] && <span className="text-xs font-bold text-white">✓</span>}
                        </button>
                        <div className="flex-1">
                          <div className="flex items-start justify-between gap-2">
                            <p className={`font-bold ${checked[i] ? "text-neutral-400 line-through" : "text-neutral-950"}`}>
                              {i + 1}. {action.title}
                            </p>
                            <span className="shrink-0 rounded-lg bg-red-100 px-2 py-0.5 font-mono text-[10px] font-bold text-red-700">
                              {action.deadline}
                            </span>
                          </div>
                        </div>
                      </div>
                      <div className="ml-9 space-y-2">
                        {action.steps?.map((step, j) => (
                          <div key={j} className="flex items-start gap-2">
                            <span className="mt-0.5 shrink-0 font-mono text-xs text-neutral-400">{j+1}.</span>
                            <p className="text-sm leading-relaxed text-neutral-700">{step}</p>
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Questions for your accountant
                    </p>
                    <p className="mt-1 text-sm text-blue-800">
                      Copy these and take them to your next meeting. Each one is specific to your situation.
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white transition">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i+1}</span>
                      <p className="text-sm leading-relaxed text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Key dates for your calendar
              </p>
              <h2 className="mb-4 font-serif text-lg font-bold text-neutral-950">
                Add these now — don't rely on memory
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Super — confirm TSB projection with accountant</p>
                    <p className="text-xs text-neutral-500">Review projected TSB at 30 June 2026 to confirm sequencing eligibility.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    1 Jun 2026
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Super — contribute $120,000 before 30 June</p>
                    <p className="text-xs text-neutral-500">Contribute annual NCC cap under 2025-26 rules for sequencing.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    25 Jun 2026
                  </span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                  <div>
                    <p className="text-sm font-semibold text-neutral-900">Super — trigger $390,000 bring-forward from 1 July</p>
                    <p className="text-xs text-neutral-500">New indexed bring-forward available from 1 July under 2026-27 rules.</p>
                  </div>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">
                    2 Jul 2026
                  </span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Downloaded — open the .ics file to add to your calendar" : "📅 Add all dates to Apple / Google / Outlook calendar →"}
              </button>
            </div>

            {/* YOUR FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your eight personalised documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything you need — in one place
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each document is built around your specific ATO position.
                Start with File 02 — it has your exact numbers.
                Files 06–08 are exclusive to this plan.
              </p>
              <div className="space-y-2">
                {FILES.map((f, i) => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    i === 1 ? "border-neutral-900 bg-neutral-950"
                    : f.tier === 2 ? "border-blue-100 bg-blue-50"
                    : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {i === 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && i !== 1 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${i === 1 ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${i === 1 ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/au/bring-forward-window/${f.slug}`}
                      target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        i === 1 ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                        : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* CLOSE — start here end here */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">One thing to do today</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — your exact numbers are in there.
                Forward File 05 to your accountant.
                Work through the checklist above.
                {daysToDeadline} days to 30 June 2026.
              </p>
              <div className="flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  ⬇ Save as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📅 Add to calendar
                </button>
                <button onClick={handleCopy}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 hover:bg-neutral-800 transition">
                  📋 Copy accountant questions
                </button>
              </div>
            </div>



            {/* CROSSLINK */}
            
            <div className="no-print rounded-2xl border border-neutral-100 bg-neutral-50 p-5">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Also relevant</p>
              <p className="mb-1 text-sm font-bold text-neutral-950">Also check your Division 296 position — balances approaching $3M need both analysed together.</p>
              <p className="mb-2 text-xs text-neutral-600">If your TSB is approaching or above $3 million, Division 296 tax will apply from 1 July 2026. Contributing more super may increase your Division 296 liability. Check both positions before acting.</p>
              <Link href="/au/check/div296-wealth-eraser" className="font-mono text-xs font-bold text-neutral-700 underline hover:text-neutral-950">
                Check your Division 296 position →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice. TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified Australia tax adviser before making financial decisions.
            Based on ATO guidance April 2026.{" "}
            <a href="https://www.ato.gov.au/individuals-and-families/super-for-individuals-and-families/super/growing-and-keeping-track-of-your-super/caps-limits-and-tax-on-super-contributions/non-concessional-contributions-cap" target="_blank" rel="noopener noreferrer" className="underline">ATO — Non-concessional contributions cap</a> · <a href="https://www.ato.gov.au/tax-rates-and-codes/key-superannuation-rates-and-thresholds/contributions-caps" target="_blank" rel="noopener noreferrer" className="underline">ATO — Key superannuation rates and thresholds (2026-27)</a>
          </p>
        </div>

      </main>
    </div>
  );
}
