"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: medicare-levy-surcharge-trap · Tier 1 Success Page
// Regenerate: npx ts-node cole/scripts/cole-generate.ts au-medicare-levy-surcharge-trap
import { useEffect, useState } from "react";
import Link from "next/link";
// ── FILE LIST ─────────────────────────────────────────────────────────────────
const FILES = [
  {
    "num": "01",
    "slug": "mls-01",
    "name": "Your MLS Liability Calculation",
    "desc": "Exact MLS payable and comparison with hospital cover cost.",
    "tier": 1,
    "start": false
  },
  {
    "num": "02",
    "slug": "mls-02",
    "name": "Cover Timing Strategy",
    "desc": "How to time hospital cover to minimise MLS for the current year.",
    "tier": 1,
    "start": true
  },
  {
    "num": "03",
    "slug": "mls-03",
    "name": "Threshold Optimisation Guide",
    "desc": "Strategies to reduce MLS income below the threshold.",
    "tier": 1,
    "start": false
  },
  {
    "num": "04",
    "slug": "mls-04",
    "name": "Cost vs Tax Comparison",
    "desc": "The exact dollar comparison between hospital cover and MLS for your income.",
    "tier": 1,
    "start": false
  },
  {
    "num": "05",
    "slug": "mls-05",
    "name": "Your Accountant Brief",
    "desc": "MLS questions to take before lodging your tax return.",
    "tier": 1,
    "start": false
  }
];
// ── TYPES ─────────────────────────────────────────────────────────────────────
interface Assessment {
  mlsStatus: string | string[] | { title: string; deadline: string; steps: string[] }[];
  incomeForMLSPurposes: string | string[] | { title: string; deadline: string; steps: string[] }[];
  surchargeRateTier: string | string[] | { title: string; deadline: string; steps: string[] }[];
  estimatedMLSPayable: string | string[] | { title: string; deadline: string; steps: string[] }[];
  coverCostEstimate: string | string[] | { title: string; deadline: string; steps: string[] }[];
  netSavingFromCover: string | string[] | { title: string; deadline: string; steps: string[] }[];
  coverTimingStrategy: string | string[] | { title: string; deadline: string; steps: string[] }[];
  thresholdPosition: string | string[] | { title: string; deadline: string; steps: string[] }[];
  strongestRiskTrigger: string | string[] | { title: string; deadline: string; steps: string[] }[];
  confidenceLevel: string | string[] | { title: string; deadline: string; steps: string[] }[];
  firstAction: string | string[] | { title: string; deadline: string; steps: string[] }[];
  accountantQuestions: string[];
  [key: string]: unknown;
}
// ── PAGE ──────────────────────────────────────────────────────────────────────
export default function SuccessAssess() {
  const [firstName,  setFirstName]  = useState("your");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number, boolean>>({});
  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2026-10-31T23:59:59.000+11:00").getTime() - Date.now()) / 86_400_000
  ));
  useEffect(() => { init(); }, []);
  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name        = "your";
    if (sessionId) {
      try {
        const res  = await fetch(`/api/get-session?id=${sessionId}`);
        const data = await res.json();
        if (data.firstName) name = data.firstName;
        setFirstName(name);
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }
  async function generateAssessment(name: string) {
    setLoading(true);
    try {
      const annual_income = sessionStorage.getItem("medicare-levy-surcharge-trap_annual_income") || "band_93_108";
      const has_hospital_cover = sessionStorage.getItem("medicare-levy-surcharge-trap_has_hospital_cover") || "false";
      const is_family = sessionStorage.getItem("medicare-levy-surcharge-trap_is_family") || "false";
      const status = sessionStorage.getItem("medicare-levy-surcharge-trap_status") || "SURCHARGE APPLIES";
      const mls_annual = sessionStorage.getItem("medicare-levy-surcharge-trap_mls_annual") || "1000";
      const tier = sessionStorage.getItem("medicare-levy-surcharge-trap_tier") || "67";
      const prompt = `You are a Australia ATO tax expert. Write a personalised assessment for ${name !== "your" ? name : "this taxpayer"}.

Their answers:
- Income band for MLS purposes: ${annual_income}\n- Has appropriate hospital cover: ${has_hospital_cover}\n- Family or single assessment: ${is_family}\n- MLS verdict status: ${status}\n- Estimated annual MLS exposure (AUD): ${mls_annual}\n- Product tier purchased: ${tier}

Rules:
- Use Australia tax terminology throughout
- Reference ATO rules, thresholds, and legislation
- Use their specific numbers — do not give generic advice
- Be direct and actionable
- If their name is provided, use it naturally

Respond ONLY with a valid JSON object. No markdown. No backticks. No preamble. Just the JSON.

Required fields:
"mlsStatus": "personalised value based on their answers",
"incomeForMLSPurposes": "personalised value based on their answers",
"surchargeRateTier": "personalised value based on their answers",
"estimatedMLSPayable": "personalised value based on their answers",
"coverCostEstimate": "personalised value based on their answers",
"netSavingFromCover": "personalised value based on their answers",
"coverTimingStrategy": "personalised value based on their answers",
"thresholdPosition": "personalised value based on their answers",
"strongestRiskTrigger": "personalised value based on their answers",
"confidenceLevel": "personalised value based on their answers",
"firstAction": "personalised value based on their answers"
"accountantQuestions": ["question 1 specific to their situation", "question 2", "question 3"]

For accountantQuestions generate exactly 3 questions for tier 1 or 5 for tier 2, each directly referencing their specific inputs.
For any array fields use actual JSON arrays.
For action items (if included) use objects: { "title": "...", "deadline": "...", "steps": ["..."] }`;

      const res  = await fetch("https://api.anthropic.com/v1/messages", {
        method:  "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          model:      "claude-sonnet-4-20250514",
          max_tokens: 1200,
          messages:   [{ role: "user", content: prompt }],
        }),
      });
      const data  = await res.json();
      const text  = data.content?.[0]?.text ?? "";
      const clean = text.replace(/```json|```/g, "").trim();
      setAssessment(JSON.parse(clean));
    } catch {
      setAssessment(buildFallback(name));
    } finally {
      setLoading(false);
    }
  }
  function buildFallback(name: string): Assessment {
    const n = name !== "your" ? name : "Your";
    const result: Record<string, unknown> = {};
    result["mlsStatus"] = "Preparing " + n + "'s mlsStatus...";
    result["incomeForMLSPurposes"] = "Preparing " + n + "'s incomeForMLSPurposes...";
    result["surchargeRateTier"] = "Preparing " + n + "'s surchargeRateTier...";
    result["estimatedMLSPayable"] = "Preparing " + n + "'s estimatedMLSPayable...";
    result["coverCostEstimate"] = "Preparing " + n + "'s coverCostEstimate...";
    result["netSavingFromCover"] = "Preparing " + n + "'s netSavingFromCover...";
    result["coverTimingStrategy"] = "Preparing " + n + "'s coverTimingStrategy...";
    result["thresholdPosition"] = "Preparing " + n + "'s thresholdPosition...";
    result["strongestRiskTrigger"] = "Preparing " + n + "'s strongestRiskTrigger...";
    result["confidenceLevel"] = "Preparing " + n + "'s confidenceLevel...";
    result["firstAction"] = "Preparing " + n + "'s firstAction...";
    result["accountantQuestions"] = [];
    return result as Assessment;
  }
  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const annual_income = sessionStorage.getItem("medicare-levy-surcharge-trap_annual_income") || "band_93_108";
    const has_hospital_cover = sessionStorage.getItem("medicare-levy-surcharge-trap_has_hospital_cover") || "false";
    const is_family = sessionStorage.getItem("medicare-levy-surcharge-trap_is_family") || "false";
    const status = sessionStorage.getItem("medicare-levy-surcharge-trap_status") || "SURCHARGE APPLIES";
    const mls_annual = sessionStorage.getItem("medicare-levy-surcharge-trap_mls_annual") || "1000";
    const tier = sessionStorage.getItem("medicare-levy-surcharge-trap_tier") || "67";
    function relativeDate(daysFromNow: number): string {
      const d = new Date(Date.now() + daysFromNow * 86_400_000);
      return d.toISOString().split("T")[0].replace(/-/g,"");
    }
    const ics = [
      "BEGIN:VCALENDAR",
      "VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN",
      "METHOD:PUBLISH",
      `X-WR-CALNAME:Medicare Levy Surcharge Trap Engine — Action Dates`,
      "BEGIN:VEVENT",
      `UID:mls-cover-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20260615"}`,
      `DTEND;VALUE=DATE:${"20260615"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Private health — get hospital cover before 30 June",
      "DESCRIPTION:Ensure hospital cover is in place before 30 June to avoid full-year MLS.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:mls-return-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20261031"}`,
      `DTEND;VALUE=DATE:${"20261031"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Tax Return Due — 31 October 2026",
      "DESCRIPTION:MLS assessed in annual individual tax return.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");
    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url  = URL.createObjectURL(blob);
    const a    = document.createElement("a");
    a.href     = url;
    a.download = "medicare-levy-surcharge-trap-deadlines.ics";
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    setCalDone(true);
  }
  async function handleCopy() {
    if (!assessment) return;
    const qs = Array.isArray(assessment.accountantQuestions)
      ? (assessment.accountantQuestions as string[]).map((q, i) => `${i + 1}. "${q}"`).join("\n")
      : "";
    await navigator.clipboard.writeText(
      `Medicare Levy Surcharge Trap Engine — questions for my accountant:\n\n${qs}\n\nPrepared by TaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }
  const greeting = firstName !== "your" ? `${firstName}, your` : "Your";
  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`
        @media print {
          .no-print { display: none !important; }
          body { font-size: 12px; color: #000; }
          a[href]:after { content: " (" attr(href) ")"; font-size: 9px; color: #555; word-break: break-all; }
          a[href^="#"]:after, a[href^="javascript"]:after { content: ""; }
          .print-section { page-break-inside: avoid; }
        }
      `}</style>
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <span className="font-mono text-xs text-neutral-400">Australia · Medicare Levy Surcharge Trap Engine</span>
        </div>
      </nav>
      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">
        {/* CONFIRMATION */}
        <div className="print-section rounded-2xl border border-emerald-200 bg-emerald-50 px-6 py-5">
          <div className="flex items-start justify-between gap-4">
            <div>
              <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
                Payment confirmed · Your MLS Avoidance Plan · $67
              </p>
              <h1 className="mt-1 font-serif text-2xl font-bold text-neutral-950">
                {greeting} Medicare Levy Surcharge Trap Engine Assessment
              </h1>
              <p className="mt-1 text-sm text-emerald-800">
                A personal assessment built around your specific position — not a generic guide.
              </p>
            </div>
            <button onClick={() => window.print()}
              className="no-print shrink-0 rounded-xl border-2 border-neutral-950 bg-white px-4 py-2.5 text-sm font-bold text-neutral-950 transition hover:bg-neutral-950 hover:text-white whitespace-nowrap">
              ⬇ Save as PDF
            </button>
          </div>
        </div>
        {/* DEADLINE BAR */}
        <div className="print-section flex items-center justify-between rounded-xl bg-red-700 px-5 py-3">
          <span className="text-sm font-bold text-white">🔴 {daysToDeadline} days to 31 October 2026</span>
          <span className="font-mono text-sm font-bold text-white">31 October 2026</span>
        </div>
        {/* LOADING */}
        {loading ? (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-medium text-neutral-600">Preparing your assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Building around your specific numbers</p>
          </div>
        ) : assessment && (
          <>
            {/* ASSESSMENT FIELDS */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-3 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your Australia tax position
              </p>
              {Object.entries(assessment)
                .filter(([k]) => !["accountantQuestions","actions","weekPlan"].includes(k))
                .slice(0, 6)
                .map(([key, value]) => (
                  <div key={key} className="mb-3 rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                    <p className="font-mono text-[10px] uppercase tracking-widest text-neutral-400 mb-1">
                      {key.replace(/_/g," ")}
                    </p>
                    <p className="text-sm font-semibold text-neutral-900">{String(value)}</p>
                  </div>
                ))}
            </div>

            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && assessment.accountantQuestions.length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Three questions for your accountant
                    </p>
                    <p className="mt-0.5 text-xs text-blue-600">
                      Full brief in <a href="/files/au/medicare-levy-surcharge-trap/mls-05"
                        target="_blank" rel="noopener noreferrer" className="font-semibold underline">File 05 →</a>
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 transition hover:bg-blue-700 hover:text-white">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i + 1}</span>
                      <p className="text-sm text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}
            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Your action deadlines</p>
              <h2 className="mb-3 font-serif text-lg font-bold text-neutral-950">Add these to your calendar now</h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-2.5">
                  <span className="text-sm text-neutral-700">Private health — get hospital cover before 30 June</span>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">15 Jun 2026</span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-2.5">
                  <span className="text-sm text-neutral-700">Tax Return Due — 31 October 2026</span>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">31 Oct 2026</span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone ? "✓ Calendar downloaded — open in Apple / Google / Outlook" : "📅 Add all deadlines to my calendar →"}
              </button>
              <p className="mt-2 text-center text-xs text-neutral-400">Works with Apple Calendar · Google Calendar · Outlook</p>
            </div>
            {/* FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your five documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">Everything in one place — start with File 02</h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each file opens in your browser and saves as a clean PDF.
                
              </p>
              <div className="space-y-2">
                {FILES.map(f => (
                  <div key={f.num} className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                    f.start ? "border-neutral-900 bg-neutral-950" : f.tier === 2 ? "border-blue-100 bg-blue-50" : "border-neutral-100 bg-neutral-50"
                  }`}>
                    <div>
                      {f.start && <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">Start here</span>}
                      {f.tier === 2 && <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">Plan only</span>}
                      <p className={`text-sm font-semibold ${f.start ? "text-white" : "text-neutral-950"}`}>{f.num} — {f.name}</p>
                      <p className={`text-xs ${f.start ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>{f.desc}</p>
                    </div>
                    <a href={`/files/au/medicare-levy-surcharge-trap/${f.slug}`} target="_blank" rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        f.start ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200" : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>Open →</a>
                  </div>
                ))}
              </div>
            </div>
            {/* START HERE END HERE */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Start here. End here.</p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — it has your exact numbers. Forward File 05 to your accountant.
                Act before 31 October 2026.
              </p>
              <div className="mb-4 flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 transition hover:bg-neutral-800">
                  ⬇ Save this page as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 transition hover:bg-neutral-800">
                  📅 Calendar →
                </button>
              </div>
              <div className="rounded-xl border border-neutral-800 bg-neutral-900 px-4 py-3">
                <div className="flex items-center justify-between">
                  <span className="text-sm text-neutral-400">Days remaining</span>
                  <span className="font-mono text-lg font-bold text-red-400">{daysToDeadline} days</span>
                </div>
                <div className="mt-2 h-1.5 rounded-full bg-neutral-800">
                  <div className="h-1.5 rounded-full bg-red-500 transition-all"
                    style={{ width: `${Math.min(100, Math.max(5, ((365 - daysToDeadline) / 365) * 100))}%` }} />
                </div>
                <p className="mt-2 text-xs text-neutral-500">
                  31 October 2026 · No backdating after this date ·{" "}
                  <a href="https://www.ato.gov.au/individuals-and-families/medicare-and-private-health-insurance/medicare-levy-surcharge" target="_blank" rel="noopener noreferrer"
                    className="underline hover:text-neutral-400 transition">ATO source</a>
                </p>
              </div>
            </div>

            {/* UPGRADE — tier 1 only */}
            <div className="no-print rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">Want the full implementation plan?</p>
              <p className="mb-1 font-serif text-lg font-bold text-neutral-950">Upgrade to Your Income and Insurance Optimisation System</p>
              <p className="mb-3 text-sm leading-relaxed text-neutral-600">Full income and tax optimisation, family structuring strategy, and insurance and tax integration plan.</p>
              <Link href="/au/check/medicare-levy-surcharge-trap"
                className="font-mono text-xs font-bold text-neutral-700 underline transition hover:text-neutral-950">
                Get Your Income and Insurance Optimisation System — $147 →
              </Link>
            </div>
          </>
        )}
        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice.
            TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified Australia tax adviser before making financial decisions.
            Based on ATO guidance April 2026.{" "}
            <a href="https://www.ato.gov.au/individuals-and-families/medicare-and-private-health-insurance/medicare-levy-surcharge" target="_blank" rel="noopener noreferrer" className="underline hover:text-neutral-700">ATO — Medicare Levy Surcharge</a>
          </p>
        </div>
      </main>
    </div>
  );
}
