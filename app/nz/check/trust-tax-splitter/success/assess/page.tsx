"use client";
// AUTO-GENERATED BY COLE — do not edit manually
// Product: trust-tax-splitter · Tier 1 Success Page
// Regenerate: npx ts-node cole/scripts/cole-generate.ts nz-trust-tax-splitter

import { useEffect, useState } from "react";
import Link from "next/link";

// ── FILE LIST ─────────────────────────────────────────────────────────────────

const FILES = [
  {
    "num": "01",
    "slug": "tt-01",
    "name": "Your Trust Tax Leakage Report",
    "desc": "How much extra tax your trust is paying — and the legal options to reduce it.",
    "tier": 1,
    "start": false
  },
  {
    "num": "02",
    "slug": "tt-02",
    "name": "Beneficiary Distribution Strategy",
    "desc": "Allocate trust income correctly to achieve the lowest combined tax rate.",
    "tier": 1,
    "start": true
  },
  {
    "num": "03",
    "slug": "tt-03",
    "name": "Student Optimisation Guide",
    "desc": "How to use adult student beneficiaries to access the 10.5% tax bracket.",
    "tier": 1,
    "start": false
  },
  {
    "num": "04",
    "slug": "tt-04",
    "name": "Trustee Resolution Template Guide",
    "desc": "What your trustee resolution must include and when it must be signed.",
    "tier": 1,
    "start": false
  },
  {
    "num": "05",
    "slug": "tt-05",
    "name": "Your Accountant Brief",
    "desc": "Print and take to your pre-year-end meeting — trust tax questions your adviser must answer.",
    "tier": 1,
    "start": false
  }
];

// ── TYPES ─────────────────────────────────────────────────────────────────────

interface Assessment {
  status: string | string[] | { title: string; deadline: string; steps: string[] }[];
  trustTaxRate: string | string[] | { title: string; deadline: string; steps: string[] }[];
  taxLeakage: string | string[] | { title: string; deadline: string; steps: string[] }[];
  distributionOpportunity: string | string[] | { title: string; deadline: string; steps: string[] }[];
  minorBeneficiaryRisk: string | string[] | { title: string; deadline: string; steps: string[] }[];
  firstAction: string | string[] | { title: string; deadline: string; steps: string[] }[];
  resolutionDeadline: string | string[] | { title: string; deadline: string; steps: string[] }[];
  accountantQuestions: string | string[] | { title: string; deadline: string; steps: string[] }[];
  [key: string]: unknown;
}

// ── PAGE ──────────────────────────────────────────────────────────────────────

export default function SuccessAssess() {
  const [firstName,  setFirstName]  = useState("your");
  const [assessment, setAssessment] = useState<Assessment | null>(null);
  const [loading,    setLoading]    = useState(true);
  const [copied,     setCopied]     = useState(false);
  const [calDone,    setCalDone]    = useState(false);
  const [checked,    setChecked]    = useState<Record<number, boolean>>({});

  const daysToDeadline = Math.max(0, Math.floor(
    (new Date("2027-03-31T23:59:59.000+13:00").getTime() - Date.now()) / 86_400_000
  ));

  useEffect(() => { init(); }, []);

  async function init() {
    const params    = new URLSearchParams(window.location.search);
    const sessionId = params.get("session_id");
    let name        = "your";
    if (sessionId) {
      try {
        const res  = await fetch(`/api/get-session?id=${sessionId}`);
        const data = await res.json();
        if (data.firstName) name = data.firstName;
        setFirstName(name);
      } catch { /* non-fatal */ }
    }
    await generateAssessment(name);
  }

  async function generateAssessment(name: string) {
    setLoading(true);
    try {
      const tt_income = sessionStorage.getItem("trust-tax-splitter_tt_income") || "20000";
      const tt_beneficiaries = sessionStorage.getItem("trust-tax-splitter_tt_beneficiaries") || "adults";
      const tt_company_compare = sessionStorage.getItem("trust-tax-splitter_tt_company_compare") || "false";
      const tt_status = sessionStorage.getItem("trust-tax-splitter_tt_status") || "approaching";

      const prompt = `You are a UK tax expert. Write a personalised assessment for ${name !== "your" ? name : "this taxpayer"}.

Their data:
- Trust income: ${tt_income}\n- Beneficiary type: ${tt_beneficiaries}\n- Company comparison wanted: ${tt_company_compare}\n- Status: ${tt_status}

Write a personalised, specific assessment. Use their name if provided. Reference their specific numbers. Be direct and actionable — not generic.

Respond ONLY with a valid JSON object. No markdown. No backticks. No preamble. Just JSON with these fields:
"status": "...", "trustTaxRate": "...", "taxLeakage": "...", "distributionOpportunity": "...", "minorBeneficiaryRisk": "...", "firstAction": "...", "resolutionDeadline": "...", "accountantQuestions": "..."

For array fields use actual arrays. For action items use objects with title, deadline, steps array.`;

      const res  = await fetch("https://api.anthropic.com/v1/messages", {
        method:  "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          model:      "claude-sonnet-4-20250514",
          max_tokens: 1200,
          messages:   [{ role: "user", content: prompt }],
        }),
      });
      const data   = await res.json();
      const text   = data.content?.[0]?.text ?? "";
      const clean  = text.replace(/```json|```/g, "").trim();
      setAssessment(JSON.parse(clean));
    } catch {
      // Fallback assessment if Claude API fails
      setAssessment(buildFallback(name));
    } finally {
      setLoading(false);
    }
  }

  function buildFallback(name: string): Assessment {
    const displayName = name !== "your" ? name : "Your";
    const result: Record<string, unknown> = {};
    result["status"] = "status — building for " + displayName;
    result["trustTaxRate"] = "trustTaxRate — building for " + displayName;
    result["taxLeakage"] = "taxLeakage — building for " + displayName;
    result["distributionOpportunity"] = "distributionOpportunity — building for " + displayName;
    result["minorBeneficiaryRisk"] = "minorBeneficiaryRisk — building for " + displayName;
    result["firstAction"] = "firstAction — building for " + displayName;
    result["resolutionDeadline"] = "resolutionDeadline — building for " + displayName;
    result["accountantQuestions"] = [];
    return result as Assessment;
  }

  function handleCalendar() {
    const now = new Date().toISOString().replace(/[-:]/g,"").split(".")[0] + "Z";
    const tt_income = sessionStorage.getItem("trust-tax-splitter_tt_income") || "20000";
    const tt_beneficiaries = sessionStorage.getItem("trust-tax-splitter_tt_beneficiaries") || "adults";
    const tt_company_compare = sessionStorage.getItem("trust-tax-splitter_tt_company_compare") || "false";
    const tt_status = sessionStorage.getItem("trust-tax-splitter_tt_status") || "approaching";

    // Helper to format relative dates
    function relativeDate(daysFromNow: number): string {
      const d = new Date(Date.now() + daysFromNow * 86_400_000);
      return d.toISOString().split("T")[0].replace(/-/g,"");
    }

    const ics = [
      "BEGIN:VCALENDAR",
      "VERSION:2.0",
      "PRODID:-//TaxCheckNow//COLE//EN",
      "CALSCALE:GREGORIAN",
      "METHOD:PUBLISH",
      `X-WR-CALNAME:Trust Tax Splitter — Action Dates`,
      "BEGIN:VEVENT",
      `UID:tt-review-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${relativeDate(14)}`,
      `DTEND;VALUE=DATE:${relativeDate(14)}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Trust — Pre-year-end distribution review",
      "DESCRIPTION:Review trust income and identify beneficiaries for distribution before 31 March.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:tt-resolution-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270320"}`,
      `DTEND;VALUE=DATE:${"20270320"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:Trust — Pass trustee resolutions",
      "DESCRIPTION:All trustee resolutions for 2026/27 distributions must be signed before 31 March 2027.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "BEGIN:VEVENT",
      `UID:tt-yearend-${Date.now()}@taxchecknow.com`,
      `DTSTART;VALUE=DATE:${"20270331"}`,
      `DTEND;VALUE=DATE:${"20270331"}`,
      `DTSTAMP:${now}`,
      "SUMMARY:NZ Tax Year End — 31 March 2027",
      "DESCRIPTION:Trust distributions must be completed. Late distributions cannot be backdated.",
      "STATUS:CONFIRMED",
      "END:VEVENT",
      "END:VCALENDAR",
    ].join("\r\n");

    const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
    const url  = URL.createObjectURL(blob);
    const a    = document.createElement("a");
    a.href     = url;
    a.download = "trust-tax-splitter-deadlines.ics";
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    setCalDone(true);
  }

  async function handleCopy() {
    if (!assessment) return;
    const questions = Array.isArray(assessment.accountantQuestions)
      ? (assessment.accountantQuestions as string[]).map((q, i) => `${i + 1}. "${q}"`).join("\n")
      : String(assessment.accountantQuestions);
    await navigator.clipboard.writeText(
      `Trust Tax Splitter — questions for my accountant:\n\n${questions}\n\nPrepared by TaxCheckNow · taxchecknow.com`
    );
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  }

  const greeting = firstName !== "your"
    ? `${firstName}, your`
    : "Your";

  return (
    <div className="min-h-screen bg-neutral-50 print:bg-white">
      <style>{`
        @media print {
          .no-print { display: none !important; }
          body { font-size: 12px; color: #000; }
          a[href]:after { content: " (" attr(href) ")"; font-size: 9px; color: #555; word-break: break-all; }
          a[href^="#"]:after, a[href^="javascript"]:after { content: ""; }
          .print-section { page-break-inside: avoid; }
        }
      `}</style>

      {/* NAV */}
      <nav className="no-print border-b border-neutral-200 bg-white px-6 py-4">
        <div className="mx-auto flex max-w-3xl items-center justify-between">
          <Link href="/" className="font-serif text-lg font-bold text-neutral-950">TaxCheckNow</Link>
          <span className="font-mono text-xs text-neutral-400">New Zealand · Trust Tax Splitter</span>
        </div>
      </nav>

      <main className="mx-auto max-w-3xl space-y-5 px-6 py-8">

        {/* CONFIRMATION */}
        <div className="print-section rounded-2xl border border-emerald-200 bg-emerald-50 px-6 py-5">
          <div className="flex items-start justify-between gap-4">
            <div>
              <p className="font-mono text-[10px] uppercase tracking-widest text-emerald-700">
                Payment confirmed · Your Beneficiary Distribution Pack · £67
              </p>
              <h1 className="mt-1 font-serif text-2xl font-bold text-neutral-950">
                {greeting} Trust Tax Splitter Assessment
              </h1>
              <p className="mt-1 text-sm text-emerald-800">
                A personal assessment built around your specific position — not a generic guide.
              </p>
            </div>
            <button
              onClick={() => window.print()}
              className="no-print shrink-0 rounded-xl border-2 border-neutral-950 bg-white px-4 py-2.5 text-sm font-bold text-neutral-950 transition hover:bg-neutral-950 hover:text-white whitespace-nowrap">
              ⬇ Save as PDF
            </button>
          </div>
        </div>

        {/* DEADLINE BAR */}
        <div className="print-section flex items-center justify-between rounded-xl bg-red-700 px-5 py-3">
          <span className="text-sm font-bold text-white">
            🔴 {daysToDeadline} days to 31 March 2027
          </span>
          <span className="font-mono text-sm font-bold text-white">31 March 2027</span>
        </div>

        {/* LOADING */}
        {loading ? (
          <div className="rounded-2xl border border-neutral-200 bg-white p-10 text-center">
            <div className="mx-auto mb-4 h-8 w-8 animate-spin rounded-full border-2 border-neutral-950 border-t-transparent" />
            <p className="text-sm font-medium text-neutral-600">Preparing your assessment…</p>
            <p className="mt-1 text-xs text-neutral-400">Building around your specific numbers</p>
          </div>
        ) : assessment && (
          <>
            {/* ASSESSMENT PANEL */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-3 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your compliance position
              </p>
              {Object.entries(assessment)
                .filter(([k]) => !["accountantQuestions", "actions", "weekPlan"].includes(k))
                .slice(0, 4)
                .map(([key, value]) => (
                  <div key={key} className="mb-3 rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-3">
                    <p className="font-mono text-[10px] uppercase tracking-widest text-neutral-400 mb-1">
                      {key.replace(/_/g, " ")}
                    </p>
                    <p className="text-sm font-semibold text-neutral-900">{String(value)}</p>
                  </div>
                ))}
            </div>



            {/* ACCOUNTANT QUESTIONS */}
            {Array.isArray(assessment.accountantQuestions) && (assessment.accountantQuestions as string[]).length > 0 && (
              <div className="print-section rounded-2xl border border-blue-100 bg-blue-50 p-6">
                <div className="mb-3 flex items-start justify-between gap-4">
                  <div>
                    <p className="font-mono text-[10px] uppercase tracking-widest text-blue-700">
                      Three questions for your accountant
                    </p>
                    <p className="mt-0.5 text-xs text-blue-600">
                      Full brief in{" "}
                      <a href="/files/nz/trust-tax-splitter/tt-05"
                        target="_blank" rel="noopener noreferrer"
                        className="font-semibold underline">File 05 →</a>
                    </p>
                  </div>
                  <button onClick={handleCopy}
                    className="no-print shrink-0 rounded-lg border border-blue-200 bg-white px-3 py-1.5 font-mono text-xs font-bold text-blue-700 transition hover:bg-blue-700 hover:text-white">
                    {copied ? "Copied ✓" : "Copy all →"}
                  </button>
                </div>
                <div className="space-y-2">
                  {(assessment.accountantQuestions as string[]).map((q, i) => (
                    <div key={i} className="flex items-start gap-3 rounded-xl border border-blue-100 bg-white px-4 py-3">
                      <span className="mt-0.5 shrink-0 font-mono text-xs font-bold text-blue-600">{i + 1}</span>
                      <p className="text-sm text-blue-900">"{q}"</p>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* CALENDAR */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your action deadlines
              </p>
              <h2 className="mb-3 font-serif text-lg font-bold text-neutral-950">
                Add these to your calendar now
              </h2>
              <div className="mb-4 space-y-2">
                
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-2.5">
                  <span className="text-sm text-neutral-700">Trust — Pre-year-end distribution review</span>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">This week</span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-2.5">
                  <span className="text-sm text-neutral-700">Trust — Pass trustee resolutions</span>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">20 Mar 2027</span>
                </div>
                <div className="flex items-center justify-between rounded-xl border border-neutral-100 bg-neutral-50 px-4 py-2.5">
                  <span className="text-sm text-neutral-700">NZ Tax Year End — 31 March 2027</span>
                  <span className="ml-3 shrink-0 font-mono text-xs font-bold text-neutral-500">31 Mar 2027</span>
                </div>
              </div>
              <button onClick={handleCalendar}
                className="no-print w-full rounded-xl bg-neutral-950 py-3.5 text-sm font-bold text-white transition hover:bg-neutral-800">
                {calDone
                  ? "✓ Calendar downloaded — open in Apple / Google / Outlook"
                  : "📅 Add all deadlines to my calendar →"}
              </button>
              <p className="mt-2 text-center text-xs text-neutral-400">
                Works with Apple Calendar · Google Calendar · Outlook
              </p>
            </div>

            {/* FILES */}
            <div className="print-section rounded-2xl border border-neutral-200 bg-white p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Your five documents
              </p>
              <h2 className="mb-1 font-serif text-xl font-bold text-neutral-950">
                Everything in one place — start with File 02
              </h2>
              <p className="mb-4 text-sm text-neutral-500">
                Each file opens in your browser and saves as a clean PDF.
                
              </p>
              <div className="space-y-2">
                {FILES.map(f => (
                  <div key={f.num}
                    className={`flex items-center justify-between rounded-xl border px-4 py-3 ${
                      f.start
                        ? "border-neutral-900 bg-neutral-950"
                        : f.tier === 2
                          ? "border-blue-100 bg-blue-50"
                          : "border-neutral-100 bg-neutral-50"
                    }`}>
                    <div>
                      {f.start && (
                        <span className="block font-mono text-[9px] uppercase tracking-widest text-amber-400 mb-0.5">
                          Start here
                        </span>
                      )}
                      {f.tier === 2 && (
                        <span className="block font-mono text-[9px] uppercase tracking-widest text-blue-600 mb-0.5">
                          Plan only
                        </span>
                      )}
                      <p className={`text-sm font-semibold ${f.start ? "text-white" : "text-neutral-950"}`}>
                        {f.num} — {f.name}
                      </p>
                      <p className={`text-xs ${f.start ? "text-neutral-400" : f.tier === 2 ? "text-blue-700" : "text-neutral-500"}`}>
                        {f.desc}
                      </p>
                    </div>
                    <a
                      href={`/files/nz/trust-tax-splitter/${f.slug}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      className={`no-print ml-4 shrink-0 rounded-lg border px-3 py-1.5 font-mono text-xs font-bold transition ${
                        f.start
                          ? "border-white/20 bg-white text-neutral-950 hover:bg-neutral-200"
                          : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-950 hover:text-white"
                      }`}>
                      Open →
                    </a>
                  </div>
                ))}
              </div>
            </div>

            {/* START HERE END HERE */}
            <div className="print-section rounded-2xl border-2 border-neutral-950 bg-neutral-950 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Start here. End here.
              </p>
              <p className="mb-4 text-lg font-bold leading-relaxed text-white">
                Open File 02 — it has your exact numbers.
                Forward File 05 to your accountant.
                Act before 31 March 2027.
              </p>
              <div className="mb-4 flex flex-wrap gap-3 no-print">
                <button onClick={() => window.print()}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 transition hover:bg-neutral-800">
                  ⬇ Save this page as PDF
                </button>
                <button onClick={handleCalendar}
                  className="rounded-xl border border-neutral-700 px-5 py-3 text-sm font-bold text-neutral-300 transition hover:bg-neutral-800">
                  📅 Calendar →
                </button>
              </div>
              <div className="rounded-xl border border-neutral-800 bg-neutral-900 px-4 py-3">
                <div className="flex items-center justify-between">
                  <span className="text-sm text-neutral-400">Days remaining</span>
                  <span className="font-mono text-lg font-bold text-red-400">{daysToDeadline} days</span>
                </div>
                <div className="mt-2 h-1.5 rounded-full bg-neutral-800">
                  <div className="h-1.5 rounded-full bg-red-500 transition-all"
                    style={{ width: `${Math.min(100, Math.max(5, ((365 - daysToDeadline) / 365) * 100))}%` }} />
                </div>
                <p className="mt-2 text-xs text-neutral-500">
                  31 March 2027 · No backdating after this date ·{" "}
                  <a href="https://www.ird.govt.nz/income-tax/income-tax-for-businesses-and-organisations/trust-and-estate-income" target="_blank" rel="noopener noreferrer"
                    className="underline hover:text-neutral-400 transition">
                    IRD source
                  </a>
                </p>
              </div>
            </div>


            {/* UPGRADE LINK — tier 1 only */}
            <div className="no-print rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
              <p className="mb-1 font-mono text-[10px] uppercase tracking-widest text-neutral-400">
                Want the full implementation plan?
              </p>
              <p className="mb-1 font-serif text-lg font-bold text-neutral-950">
                Upgrade to Your Trust vs Company Pivot Audit
              </p>
              <p className="mb-3 text-sm leading-relaxed text-neutral-600">
                A personalised trust audit plus company comparison, imputation credit mapping, and multi-entity optimisation strategy.
              </p>
              <Link href="/nz/check/trust-tax-splitter"
                className="font-mono text-xs font-bold text-neutral-700 underline transition hover:text-neutral-950">
                Get Your Trust vs Company Pivot Audit — £147 →
              </Link>
            </div>

          </>
        )}

        {/* DISCLAIMER */}
        <div className="rounded-xl bg-neutral-100 px-5 py-4">
          <p className="text-xs leading-relaxed text-neutral-500">
            <strong className="text-neutral-600">General information only.</strong>{" "}
            This assessment does not constitute financial, tax or legal advice.
            TaxCheckNow is not a regulated financial adviser.
            Always consult a qualified UK tax adviser before making financial decisions.
            Based on IRD guidance April 2026.{" "}
            <a href="https://www.ird.govt.nz/income-tax/income-tax-for-businesses-and-organisations/trust-and-estate-income" target="_blank" rel="noopener noreferrer" className="underline hover:text-neutral-700">IRD — Trust and estate income</a> · <a href="https://www.ird.govt.nz/income-tax/income-tax-for-individuals/types-of-individual-income/trust-income" target="_blank" rel="noopener noreferrer" className="underline hover:text-neutral-700">IRD — Minor beneficiary rule</a>
          </p>
        </div>

      </main>
    </div>
  );
}
