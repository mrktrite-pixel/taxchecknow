// ─────────────────────────────────────────────────────────────────────────────
// COLE Generator — generate-product-files
// Status: PLACEHOLDER — not yet built
// ─────────────────────────────────────────────────────────────────────────────

import type { ProductConfig } from "../types/product-config";

export function generateProductFiles(config: ProductConfig): string {
  // TODO: build this generator
  // Returns the full file content as a string
  // COLE writes it to the correct path
  throw new Error("Generator not yet built: generate-product-files");
}
